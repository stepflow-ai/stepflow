plugins:
  builtin:
    type: builtin
  python:
    type: stepflow
    command: uv
    args: [--project, "../../sdks/python", run, stepflow_worker]
routes:
  "/python/{component}":
    - plugin: python
  "/builtin/{component}":
    - plugin: builtin
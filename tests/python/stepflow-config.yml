plugins:
  builtin:
    type: builtin
  python:
    type: pull
    command: uv
    args: [--project, "../../sdks/python/stepflow-py", run, stepflow_worker]
    queueName: python
routes:
  "/python/{component}":
    - plugin: python
  "/builtin/{component}":
    - plugin: builtin
input_schema:
  object:
    properties:
      in1:
        type: number
      in2:
        type: string
steps:
  # 
  - component: mock://two_outputs
    id: s1
    args:
      foo: { input: in1 }
      bar: { literal: 42 }
  - component: mock://one_output
    id: s2
    args:
      a: { step: s1, output: a }
      b: { step: s1, output: b }
      c: { literal: 5 }
outputs:
  out1: { step: s1, output: a }
  out2: { step: s1, output: b }
  outLiteral: { literal: 5 }
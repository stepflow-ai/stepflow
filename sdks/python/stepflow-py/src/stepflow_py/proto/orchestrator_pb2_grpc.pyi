"""
@generated by mypy-protobuf.  Do not edit manually!
isort:skip_file
Copyright 2025 DataStax Inc.

Licensed under the Apache License, Version 2.0 (the "License"); you may not
use this file except in compliance with the License. You may obtain a copy of
the License at

    http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS, WITHOUT
WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the
License for the specific language governing permissions and limitations under
the License.
"""

from collections import abc as _abc
from grpc import aio as _aio
from . import orchestrator_pb2 as _orchestrator_pb2
import abc as _abc_1
import grpc as _grpc
import sys
import typing as _typing

if sys.version_info >= (3, 11):
    from typing import Self as _Self
else:
    from typing_extensions import Self as _Self

_T = _typing.TypeVar("_T")

class _MaybeAsyncIterator(_abc.AsyncIterator[_T], _abc.Iterator[_T], metaclass=_abc_1.ABCMeta): ...

class _ServicerContext(_grpc.ServicerContext, _aio.ServicerContext):  # type: ignore[misc, type-arg]
    ...

GRPC_GENERATED_VERSION: str
GRPC_VERSION: str

class OrchestratorServiceStub:
    """Service exposed by the orchestrator for workers to interact with during
    component execution. Workers reach this via `orchestrator_service_url`
    from TaskContext.

    This must reach the orchestrator that owns the specific run — it is NOT
    interchangeable with other orchestrators (unlike WorkerService).

    gRPC only — not exposed as REST.
    """

    @_typing.overload
    def __new__(cls, channel: _grpc.Channel) -> _Self: ...
    @_typing.overload
    def __new__(cls, channel: _aio.Channel) -> OrchestratorServiceAsyncStub: ...
    SubmitRun: _grpc.UnaryUnaryMultiCallable[_orchestrator_pb2.OrchestratorSubmitRunRequest, _orchestrator_pb2.OrchestratorRunStatus]
    """Submit a sub-flow run from within a component execution."""
    GetRun: _grpc.UnaryUnaryMultiCallable[_orchestrator_pb2.OrchestratorGetRunRequest, _orchestrator_pb2.OrchestratorRunStatus]
    """Get the status of a run (optionally waiting for completion)."""
    CompleteTask: _grpc.UnaryUnaryMultiCallable[_orchestrator_pb2.CompleteTaskRequest, _orchestrator_pb2.CompleteTaskResponse]
    """Report task completion (success or failure).

    Called by workers after executing a component, regardless of how the
    task was received (PullTasks, NATS, Kafka, etc.). The worker uses the
    `orchestrator_service_url` from the task's TaskContext to reach
    the orchestrator that owns the run.
    """
    StartTask: _grpc.UnaryUnaryMultiCallable[_orchestrator_pb2.StartTaskRequest, _orchestrator_pb2.StartTaskResponse]
    """Notify the orchestrator that the worker has started executing a task.

    Called by the worker immediately before beginning component execution.
    This transitions the task from "queued" to "executing" on the
    orchestrator side, starting the heartbeat timeout. If the response
    has `timed_out = true`, the task already expired in the queue and the
    worker should skip execution.
    """
    TaskHeartbeat: _grpc.UnaryUnaryMultiCallable[_orchestrator_pb2.TaskHeartbeatRequest, _orchestrator_pb2.TaskHeartbeatResponse]
    """Send a heartbeat for an in-progress task.

    Called periodically by the worker during component execution (typically
    every 1s). Each heartbeat resets the crash-detection timer on the
    orchestrator. If heartbeats stop arriving for 5s, the orchestrator
    presumes the worker crashed and fails the task.
    """

@_typing.type_check_only
class OrchestratorServiceAsyncStub(OrchestratorServiceStub):
    """Service exposed by the orchestrator for workers to interact with during
    component execution. Workers reach this via `orchestrator_service_url`
    from TaskContext.

    This must reach the orchestrator that owns the specific run — it is NOT
    interchangeable with other orchestrators (unlike WorkerService).

    gRPC only — not exposed as REST.
    """

    def __init__(self, channel: _aio.Channel) -> None: ...
    SubmitRun: _aio.UnaryUnaryMultiCallable[_orchestrator_pb2.OrchestratorSubmitRunRequest, _orchestrator_pb2.OrchestratorRunStatus]  # type: ignore[assignment]
    """Submit a sub-flow run from within a component execution."""
    GetRun: _aio.UnaryUnaryMultiCallable[_orchestrator_pb2.OrchestratorGetRunRequest, _orchestrator_pb2.OrchestratorRunStatus]  # type: ignore[assignment]
    """Get the status of a run (optionally waiting for completion)."""
    CompleteTask: _aio.UnaryUnaryMultiCallable[_orchestrator_pb2.CompleteTaskRequest, _orchestrator_pb2.CompleteTaskResponse]  # type: ignore[assignment]
    """Report task completion (success or failure).

    Called by workers after executing a component, regardless of how the
    task was received (PullTasks, NATS, Kafka, etc.). The worker uses the
    `orchestrator_service_url` from the task's TaskContext to reach
    the orchestrator that owns the run.
    """
    StartTask: _aio.UnaryUnaryMultiCallable[_orchestrator_pb2.StartTaskRequest, _orchestrator_pb2.StartTaskResponse]  # type: ignore[assignment]
    """Notify the orchestrator that the worker has started executing a task.

    Called by the worker immediately before beginning component execution.
    This transitions the task from "queued" to "executing" on the
    orchestrator side, starting the heartbeat timeout. If the response
    has `timed_out = true`, the task already expired in the queue and the
    worker should skip execution.
    """
    TaskHeartbeat: _aio.UnaryUnaryMultiCallable[_orchestrator_pb2.TaskHeartbeatRequest, _orchestrator_pb2.TaskHeartbeatResponse]  # type: ignore[assignment]
    """Send a heartbeat for an in-progress task.

    Called periodically by the worker during component execution (typically
    every 1s). Each heartbeat resets the crash-detection timer on the
    orchestrator. If heartbeats stop arriving for 5s, the orchestrator
    presumes the worker crashed and fails the task.
    """

class OrchestratorServiceServicer(metaclass=_abc_1.ABCMeta):
    """Service exposed by the orchestrator for workers to interact with during
    component execution. Workers reach this via `orchestrator_service_url`
    from TaskContext.

    This must reach the orchestrator that owns the specific run — it is NOT
    interchangeable with other orchestrators (unlike WorkerService).

    gRPC only — not exposed as REST.
    """

    @_abc_1.abstractmethod
    def SubmitRun(
        self,
        request: _orchestrator_pb2.OrchestratorSubmitRunRequest,
        context: _ServicerContext,
    ) -> _typing.Union[_orchestrator_pb2.OrchestratorRunStatus, _abc.Awaitable[_orchestrator_pb2.OrchestratorRunStatus]]:
        """Submit a sub-flow run from within a component execution."""

    @_abc_1.abstractmethod
    def GetRun(
        self,
        request: _orchestrator_pb2.OrchestratorGetRunRequest,
        context: _ServicerContext,
    ) -> _typing.Union[_orchestrator_pb2.OrchestratorRunStatus, _abc.Awaitable[_orchestrator_pb2.OrchestratorRunStatus]]:
        """Get the status of a run (optionally waiting for completion)."""

    @_abc_1.abstractmethod
    def CompleteTask(
        self,
        request: _orchestrator_pb2.CompleteTaskRequest,
        context: _ServicerContext,
    ) -> _typing.Union[_orchestrator_pb2.CompleteTaskResponse, _abc.Awaitable[_orchestrator_pb2.CompleteTaskResponse]]:
        """Report task completion (success or failure).

        Called by workers after executing a component, regardless of how the
        task was received (PullTasks, NATS, Kafka, etc.). The worker uses the
        `orchestrator_service_url` from the task's TaskContext to reach
        the orchestrator that owns the run.
        """

    @_abc_1.abstractmethod
    def StartTask(
        self,
        request: _orchestrator_pb2.StartTaskRequest,
        context: _ServicerContext,
    ) -> _typing.Union[_orchestrator_pb2.StartTaskResponse, _abc.Awaitable[_orchestrator_pb2.StartTaskResponse]]:
        """Notify the orchestrator that the worker has started executing a task.

        Called by the worker immediately before beginning component execution.
        This transitions the task from "queued" to "executing" on the
        orchestrator side, starting the heartbeat timeout. If the response
        has `timed_out = true`, the task already expired in the queue and the
        worker should skip execution.
        """

    @_abc_1.abstractmethod
    def TaskHeartbeat(
        self,
        request: _orchestrator_pb2.TaskHeartbeatRequest,
        context: _ServicerContext,
    ) -> _typing.Union[_orchestrator_pb2.TaskHeartbeatResponse, _abc.Awaitable[_orchestrator_pb2.TaskHeartbeatResponse]]:
        """Send a heartbeat for an in-progress task.

        Called periodically by the worker during component execution (typically
        every 1s). Each heartbeat resets the crash-detection timer on the
        orchestrator. If heartbeats stop arriving for 5s, the orchestrator
        presumes the worker crashed and fails the task.
        """

def add_OrchestratorServiceServicer_to_server(servicer: OrchestratorServiceServicer, server: _typing.Union[_grpc.Server, _aio.Server]) -> None: ...

# This file was auto-generated by scripts/generate_api_client.py
# Do not edit manually - changes will be overwritten on regeneration.
#
# Generated from: openapi.json

# coding: utf-8

"""
Stepflow API

API for Stepflow flows and runs

The version of the OpenAPI document: 0.9.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations

import json
import pprint
from typing import Any, Self

from pydantic import (
    BaseModel,
    ConfigDict,
    ValidationError,
    field_validator,
    model_serializer,
)

from stepflow_py.api.models.flow_result_failed import FlowResultFailed
from stepflow_py.api.models.flow_result_success import FlowResultSuccess

FLOWRESULT_ONE_OF_SCHEMAS = ["FlowResultFailed", "FlowResultSuccess"]


class FlowResult(BaseModel):
    """
    The results of a step execution.
    """

    # data type: FlowResultSuccess
    oneof_schema_1_validator: FlowResultSuccess | None = None
    # data type: FlowResultFailed
    oneof_schema_2_validator: FlowResultFailed | None = None
    actual_instance: FlowResultFailed | FlowResultSuccess | None = None
    one_of_schemas: set[str] = {"FlowResultFailed", "FlowResultSuccess"}

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )

    @model_serializer(mode="plain")
    def _serialize(self) -> Any:
        """Serialize by returning only the actual_instance, ignoring wrapper fields."""
        return self.actual_instance

    discriminator_value_class_map: dict[str, str] = {}

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError(
                    "If a position argument is used, only 1 is allowed to set `actual_instance`"
                )
            if kwargs:
                raise ValueError(
                    "If a position argument is used, keyword arguments cannot be used."
                )
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator("actual_instance")
    def actual_instance_must_validate_oneof(cls, v):
        instance = FlowResult.model_construct()
        error_messages = []
        match = 0
        # validate data type: FlowResultSuccess
        if not isinstance(v, FlowResultSuccess):
            error_messages.append(
                f"Error! Input type `{type(v)}` is not `FlowResultSuccess`"
            )
        else:
            match += 1
        # validate data type: FlowResultFailed
        if not isinstance(v, FlowResultFailed):
            error_messages.append(
                f"Error! Input type `{type(v)}` is not `FlowResultFailed`"
            )
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError(
                "Multiple matches found when setting `actual_instance` in FlowResult with oneOf schemas: FlowResultFailed, FlowResultSuccess. Details: "
                + ", ".join(error_messages)
            )
        elif match == 0:
            # no match
            raise ValueError(
                "No match found when setting `actual_instance` in FlowResult with oneOf schemas: FlowResultFailed, FlowResultSuccess. Details: "
                + ", ".join(error_messages)
            )
        else:
            return v

    @classmethod
    def from_dict(cls, obj: str | dict[str, Any]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into FlowResultSuccess
        try:
            instance.actual_instance = FlowResultSuccess.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into FlowResultFailed
        try:
            instance.actual_instance = FlowResultFailed.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError(
                "Multiple matches found when deserializing the JSON string into FlowResult with oneOf schemas: FlowResultFailed, FlowResultSuccess. Details: "
                + ", ".join(error_messages)
            )
        elif match == 0:
            # no match
            raise ValueError(
                "No match found when deserializing the JSON string into FlowResult with oneOf schemas: FlowResultFailed, FlowResultSuccess. Details: "
                + ", ".join(error_messages)
            )
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(
            self.actual_instance.to_json
        ):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(
        self,
    ) -> dict[str, Any] | FlowResultFailed | FlowResultSuccess | None:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(
            self.actual_instance.to_dict
        ):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())

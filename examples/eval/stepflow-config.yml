plugins:
  builtin:
    type: builtin
  python:
    type: pull
    command: uv
    args: ["--project", "../../sdks/python/stepflow-py", "run", "stepflow_worker"]
    queueName: python
routes:
  "/builtin/{*component}":
    - plugin: builtin
  "/python/{*component}":
    - plugin: python

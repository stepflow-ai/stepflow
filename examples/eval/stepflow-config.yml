plugins:
  builtin:
    type: builtin
  python:
    type: stepflow
    command: uv
    args: ["--project", "../../sdks/python", "run", "stepflow_worker"]
routes:
  "/builtin/{*component}":
    - plugin: builtin
  "/python/{*component}":
    - plugin: python

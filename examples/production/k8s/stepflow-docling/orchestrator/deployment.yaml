# Copyright 2025 DataStax Inc.
#
# Licensed under the Apache License, Version 2.0 (the "License"); you may not
# use this file except in compliance with the License. You may obtain a copy of
# the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS, WITHOUT
# WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the
# License for the specific language governing permissions and limitations under
# the License.

# Orchestrator pod: docling-facade (port 5001) + stepflow-server (port 7840)
# The facade presents a docling-serve compatible API and delegates to the
# co-located stepflow-server via localhost.

apiVersion: apps/v1
kind: Deployment
metadata:
  name: docling-orchestrator
  namespace: stepflow-docling
  labels:
    app: docling-orchestrator
    component: orchestration
spec:
  replicas: 1
  selector:
    matchLabels:
      app: docling-orchestrator
  template:
    metadata:
      labels:
        app: docling-orchestrator
        component: orchestration
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      volumes:
      - name: config
        configMap:
          name: docling-orchestrator-config
      # Wait for the docling worker to be reachable via the load balancer
      # before starting stepflow-server (which initializes the plugin on startup).
      initContainers:
      - name: wait-for-worker
        image: docker.io/library/busybox:1.37
        imagePullPolicy: IfNotPresent
        securityContext:
          allowPrivilegeEscalation: false
        command: ['sh', '-c', 'echo "Waiting for docling-lb..."; until wget -qO- http://docling-lb.stepflow-docling.svc.cluster.local:8080/health >/dev/null 2>&1; do echo "waiting..."; sleep 5; done; echo "Worker reachable!"']
      containers:
      # Container 1: docling-facade — docling-serve compatible HTTP API
      - name: docling-facade
        image: localhost/docling-facade:parity-v1
        imagePullPolicy: Never
        securityContext:
          allowPrivilegeEscalation: false
        ports:
        - name: facade
          containerPort: 5001
          protocol: TCP
        env:
        - name: STEPFLOW_SERVER_URL
          value: "http://localhost:7840"
        - name: STEPFLOW_FLOW_PATH
          value: "/app/config/docling-process-document.yaml"
        - name: FACADE_PORT
          value: "5001"
        # Pod metadata
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        # OTEL configuration
        - name: OTEL_SERVICE_NAME
          value: "docling-facade"
        - name: OTEL_RESOURCE_ATTRIBUTES
          value: "deployment.environment=kubernetes,service.namespace=stepflow-docling,service.instance.id=$(POD_NAME),k8s.pod.name=$(POD_NAME),k8s.namespace.name=$(POD_NAMESPACE)"
        volumeMounts:
        - name: config
          mountPath: /app/config
          readOnly: true
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 256Mi
        livenessProbe:
          httpGet:
            path: /health
            port: facade
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: facade
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3

      # Container 2: stepflow-server — Rust orchestrator
      - name: stepflow-server
        image: localhost/stepflow-server:alpine-0.10.0
        imagePullPolicy: Never
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
        command: ["stepflow-server"]
        args: ["--port", "7840", "--config", "/app/config/stepflow-config.yml"]
        ports:
        - name: http
          containerPort: 7840
          protocol: TCP
        env:
        # Pod metadata
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        # Stepflow logging
        - name: STEPFLOW_LOG_LEVEL
          value: "debug"
        - name: STEPFLOW_OTHER_LOG_LEVEL
          value: "warn"
        - name: STEPFLOW_LOG_FORMAT
          value: "json"
        # Stepflow OTLP
        - name: STEPFLOW_OTLP_ENDPOINT
          value: "http://otel-collector.stepflow-o11y.svc.cluster.local:4317"
        - name: STEPFLOW_TRACE_ENABLED
          value: "true"
        - name: STEPFLOW_METRICS_ENABLED
          value: "true"
        # Standard OTEL
        - name: OTEL_SERVICE_NAME
          value: "stepflow-server"
        - name: OTEL_RESOURCE_ATTRIBUTES
          value: "deployment.environment=kubernetes,service.namespace=stepflow-docling,service.instance.id=$(POD_NAME),k8s.pod.name=$(POD_NAME),k8s.namespace.name=$(POD_NAMESPACE)"
        volumeMounts:
        - name: config
          mountPath: /app/config
          readOnly: true
        resources:
          requests:
            cpu: 200m
            memory: 256Mi
          limits:
            cpu: 1000m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /api/v1/health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /api/v1/health
            port: http
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3

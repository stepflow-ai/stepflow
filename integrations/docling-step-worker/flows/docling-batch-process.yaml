name: docling-batch-process
steps:
  - id: process_all
    component: /map
    input:
      workflow: docling-process-document
      items: { $input: "documents" }
      max_concurrency: { $input: "max_concurrency" }

output:
  results: { $step: "process_all", path: "results" }

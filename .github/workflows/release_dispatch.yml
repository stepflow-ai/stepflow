name: Release - Dispatch

on:
  pull_request:
    types: [closed]
    branches: [main]

permissions:
  contents: read
  actions: write

jobs:
  dispatch-stepflow-release:
    name: Dispatch Stepflow Release Build
    runs-on: ubuntu-22.04
    if: github.event.pull_request.merged == true && contains(github.event.pull_request.labels.*.name, 'release:stepflow')

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.merge_commit_sha }}

      - name: Extract version from Cargo.toml
        id: version
        working-directory: stepflow-rs
        run: |
          VERSION=$(grep '^version = ' Cargo.toml | head -1 | sed 's/version = "\(.*\)"/\1/')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "tag=stepflow-$VERSION" >> $GITHUB_OUTPUT
          echo "Extracted version: $VERSION"

      - name: Dispatch release build
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh workflow run "Release - Stepflow" \
            --field version="${{ steps.version.outputs.version }}" \
            --field skip_tag_creation=false

      - name: Summary
        run: |
          echo "## Stepflow Release Build Dispatched" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ Dispatched release build for **Stepflow v${{ steps.version.outputs.version }}**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Tag:** ${{ steps.version.outputs.tag }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "The release build workflow will:" >> $GITHUB_STEP_SUMMARY
          echo "1. Build binaries for all platforms" >> $GITHUB_STEP_SUMMARY
          echo "2. Build and push Docker images" >> $GITHUB_STEP_SUMMARY
          echo "3. Build Python wheels for all platforms" >> $GITHUB_STEP_SUMMARY
          echo "4. Publish wheels to PyPI" >> $GITHUB_STEP_SUMMARY
          echo "5. Create git tag and GitHub release" >> $GITHUB_STEP_SUMMARY

  dispatch-python-release:
    name: Dispatch Python SDK Release
    runs-on: ubuntu-22.04
    if: github.event.pull_request.merged == true && contains(github.event.pull_request.labels.*.name, 'release:stepflow-py')

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.merge_commit_sha }}

      - name: Extract version from pyproject.toml
        id: version
        working-directory: sdks/python/stepflow-py
        run: |
          VERSION=$(grep '^version = ' pyproject.toml | head -1 | sed 's/version = "\(.*\)"/\1/')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "tag=stepflow-py-$VERSION" >> $GITHUB_OUTPUT
          echo "Extracted version: $VERSION"

      - name: Dispatch Python release build
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh workflow run "Release - Python SDK" \
            --field version="${{ steps.version.outputs.version }}" \
            --field skip_tag_creation=false

      - name: Summary
        run: |
          echo "## Python SDK Release Build Dispatched" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ Dispatched release build for **stepflow-py v${{ steps.version.outputs.version }}**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Tag:** ${{ steps.version.outputs.tag }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "The Python release workflow will:" >> $GITHUB_STEP_SUMMARY
          echo "1. Build Python wheel and source distribution" >> $GITHUB_STEP_SUMMARY
          echo "2. Run comprehensive tests" >> $GITHUB_STEP_SUMMARY
          echo "3. Publish to PyPI using Trusted Publishing" >> $GITHUB_STEP_SUMMARY
          echo "4. Create git tag and GitHub release" >> $GITHUB_STEP_SUMMARY

  dispatch-langflow-release:
    name: Dispatch Langflow Integration Release
    runs-on: ubuntu-22.04
    if: github.event.pull_request.merged == true && contains(github.event.pull_request.labels.*.name, 'release:stepflow-langflow')

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.merge_commit_sha }}

      - name: Extract version from pyproject.toml
        id: version
        working-directory: integrations/langflow
        run: |
          VERSION=$(grep '^version = ' pyproject.toml | head -1 | sed 's/version = "\(.*\)"/\1/')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "tag=stepflow-langflow-$VERSION" >> $GITHUB_OUTPUT
          echo "Extracted version: $VERSION"

      - name: Dispatch Langflow release build
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh workflow run "Release - Langflow Integration" \
            --field version="${{ steps.version.outputs.version }}" \
            --field skip_tag_creation=false

      - name: Summary
        run: |
          echo "## Langflow Integration Release Build Dispatched" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ Dispatched release build for **stepflow-langflow-integration v${{ steps.version.outputs.version }}**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Tag:** ${{ steps.version.outputs.tag }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "The Langflow release workflow will:" >> $GITHUB_STEP_SUMMARY
          echo "1. Build Python wheel and source distribution" >> $GITHUB_STEP_SUMMARY
          echo "2. Run comprehensive tests" >> $GITHUB_STEP_SUMMARY
          echo "3. Build and push Docker image" >> $GITHUB_STEP_SUMMARY
          echo "4. Publish to PyPI using Trusted Publishing" >> $GITHUB_STEP_SUMMARY
          echo "5. Create git tag and GitHub release" >> $GITHUB_STEP_SUMMARY

  dispatch-rust-sdk-release:
    name: Dispatch Rust SDK Release
    runs-on: ubuntu-22.04
    if: github.event.pull_request.merged == true && contains(github.event.pull_request.labels.*.name, 'release:rust-sdk')

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.merge_commit_sha }}

      - name: Extract version from Cargo.toml
        id: version
        working-directory: sdks/rust
        run: |
          VERSION=$(grep '^version = ' Cargo.toml | head -1 | sed 's/version = "\(.*\)"/\1/')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "tag=rust-sdk-$VERSION" >> $GITHUB_OUTPUT
          echo "Extracted version: $VERSION"

      - name: Dispatch Rust SDK release build
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh workflow run "Release - Rust SDK" \
            --field version="${{ steps.version.outputs.version }}" \
            --field skip_tag_creation=false

      - name: Summary
        run: |
          echo "## Rust SDK Release Build Dispatched" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Dispatched release build for **Rust SDK v${{ steps.version.outputs.version }}**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Tag:** ${{ steps.version.outputs.tag }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "The Rust SDK release workflow will:" >> $GITHUB_STEP_SUMMARY
          echo "1. Publish stepflow-worker and stepflow-client to crates.io" >> $GITHUB_STEP_SUMMARY
          echo "2. Create git tag and GitHub release" >> $GITHUB_STEP_SUMMARY

  dispatch-docling-release:
    name: Dispatch Docling Proto Step Worker Release
    runs-on: ubuntu-22.04
    if: github.event.pull_request.merged == true && contains(github.event.pull_request.labels.*.name, 'release:docling')

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.merge_commit_sha }}

      - name: Extract version from pyproject.toml
        id: version
        working-directory: integrations/docling-proto-step-worker
        run: |
          VERSION=$(grep '^version = ' pyproject.toml | head -1 | sed 's/version = "\(.*\)"/\1/')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "tag=stepflow-docling-$VERSION" >> $GITHUB_OUTPUT
          echo "Extracted version: $VERSION"

      - name: Dispatch Docling release build
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh workflow run "Release - Docling Proto Step Worker" \
            --field version="${{ steps.version.outputs.version }}" \
            --field skip_tag_creation=false

      - name: Summary
        run: |
          echo "## Docling Proto Step Worker Release Build Dispatched" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ Dispatched release build for **docling-proto-step-worker v${{ steps.version.outputs.version }}**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Tag:** ${{ steps.version.outputs.tag }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "The Docling release workflow will:" >> $GITHUB_STEP_SUMMARY
          echo "1. Run linting and unit tests" >> $GITHUB_STEP_SUMMARY
          echo "2. Build and push Docker images to GHCR" >> $GITHUB_STEP_SUMMARY
          echo "3. Create git tag and GitHub release" >> $GITHUB_STEP_SUMMARY
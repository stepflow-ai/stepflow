{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "StepflowConfig",
  "description": "Prefix-keyed routing configuration.\n\nKeys are single-segment path prefixes (e.g., \"/python\", \"/builtin\") or the\nroot catch-all \"/\". Multi-segment prefixes (e.g., \"/python/core\") are not\nallowed. Each plugin's registered component paths are mounted under the\nprefix. The orchestrator builds a per-plugin trie from the plugin's\ncomponent registrations.",
  "type": "object",
  "properties": {
    "workingDirectory": {
      "description": "Working directory for the configuration.\n\nIf not set, this will be the directory containing the config.",
      "type": [
        "string",
        "null"
      ]
    },
    "plugins": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/SupportedPluginConfig"
      }
    },
    "routes": {
      "description": "Prefix-to-routing rules mapping.\n\nKeys must be either \"/\" (catch-all) or a single-segment prefix like\n\"/python\", \"/builtin\". Multi-segment prefixes are rejected at build time.\nEach plugin's registered component paths are mounted under the prefix.\n\nValue: ordered list of routing rules. When multiple rules exist for a prefix,\nthey are evaluated in order — the first rule whose conditions match is used.",
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "items": {
          "$ref": "#/$defs/RouteRule"
        }
      }
    },
    "storageConfig": {
      "description": "Storage configuration. If not specified, uses in-memory storage.",
      "default": {
        "type": "inMemory"
      },
      "oneOf": [
        {
          "$ref": "#/$defs/StorageConfig"
        },
        {
          "type": "null"
        }
      ]
    },
    "leaseManager": {
      "description": "Lease manager configuration for distributed coordination.\nIf not specified, uses no-op (single orchestrator mode).",
      "default": {
        "type": "noOp"
      },
      "oneOf": [
        {
          "$ref": "#/$defs/LeaseManagerConfig"
        },
        {
          "type": "null"
        }
      ]
    },
    "recovery": {
      "description": "Recovery configuration for handling interrupted runs.",
      "default": {
        "enabled": true,
        "checkIntervalSecs": 30,
        "maxStartupRecovery": 100,
        "maxClaimsPerCheck": 10,
        "leaseTtlSecs": 30,
        "checkpointInterval": 1000
      },
      "oneOf": [
        {
          "$ref": "#/$defs/RecoveryConfig"
        },
        {
          "type": "null"
        }
      ]
    },
    "blobApi": {
      "description": "Blob API configuration.\nControls whether the orchestrator serves blob endpoints and the URL workers use.",
      "default": {
        "enabled": true
      },
      "oneOf": [
        {
          "$ref": "#/$defs/BlobApiConfig"
        },
        {
          "type": "null"
        }
      ]
    },
    "retry": {
      "description": "Retry configuration.\nControls backoff for all retries and the retry limit for transport errors\n(subprocess crash, network timeout, connection refused).",
      "default": {
        "transportMaxRetries": 3,
        "backoff": {
          "type": "fibonacci",
          "minDelayMs": 1000,
          "maxDelayMs": 10000
        }
      },
      "oneOf": [
        {
          "$ref": "#/$defs/RetryConfig"
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "required": [
    "plugins",
    "routes"
  ],
  "$defs": {
    "SupportedPluginConfig": {
      "type": "object",
      "oneOf": [
        {
          "$ref": "#/$defs/BuiltinPluginConfig"
        },
        {
          "$ref": "#/$defs/MockPlugin"
        },
        {
          "$ref": "#/$defs/McpPluginConfig"
        },
        {
          "$ref": "#/$defs/GrpcPluginConfig"
        },
        {
          "$ref": "#/$defs/NatsPluginConfig"
        }
      ],
      "discriminator": {
        "propertyName": "type",
        "mapping": {
          "builtin": "#/$defs/BuiltinPluginConfig",
          "mock": "#/$defs/MockPlugin",
          "mcp": "#/$defs/McpPluginConfig",
          "grpc": "#/$defs/GrpcPluginConfig",
          "nats": "#/$defs/NatsPluginConfig"
        }
      }
    },
    "MockComponent": {
      "type": "object",
      "properties": {
        "input_schema": {
          "$ref": "#/$defs/Schema"
        },
        "output_schema": {
          "$ref": "#/$defs/Schema"
        },
        "behaviors": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/MockComponentBehavior"
          }
        }
      },
      "required": [
        "input_schema",
        "output_schema",
        "behaviors"
      ]
    },
    "Schema": {
      "description": "A valid JSON Schema object.",
      "type": "object",
      "additionalProperties": true
    },
    "MockComponentBehavior": {
      "description": "Enumeration of behaviors for the mock components.",
      "anyOf": [
        {
          "title": "MockComponentError",
          "description": "Produce the given internal (non-flow) error.",
          "type": "object",
          "properties": {
            "error": {
              "type": "string"
            }
          },
          "required": [
            "error"
          ]
        },
        {
          "title": "MockComponentResult",
          "description": "Return the given result (success or flow-error).",
          "type": "object",
          "oneOf": [
            {
              "$ref": "#/$defs/FlowResultSuccess"
            },
            {
              "$ref": "#/$defs/FlowResultFailed"
            }
          ],
          "discriminator": {
            "propertyName": "outcome",
            "mapping": {
              "success": "#/$defs/FlowResultSuccess",
              "failed": "#/$defs/FlowResultFailed"
            }
          }
        }
      ]
    },
    "FlowResultSuccess": {
      "description": "The step execution was successful.",
      "type": "object",
      "properties": {
        "outcome": {
          "type": "string",
          "const": "success",
          "default": "success"
        },
        "result": {
          "$ref": "#/$defs/Value"
        }
      },
      "required": [
        "outcome",
        "result"
      ]
    },
    "Value": {
      "description": "Any JSON value (object, array, string, number, boolean, or null)"
    },
    "FlowResultFailed": {
      "description": "The step failed with the given error.",
      "type": "object",
      "properties": {
        "outcome": {
          "type": "string",
          "const": "failed",
          "default": "failed"
        },
        "error": {
          "$ref": "#/$defs/FlowError"
        }
      },
      "required": [
        "outcome",
        "error"
      ]
    },
    "FlowError": {
      "description": "An error reported from within a flow or step.",
      "type": "object",
      "properties": {
        "code": {
          "$ref": "#/$defs/TaskErrorCode"
        },
        "message": {
          "type": "string"
        },
        "data": {
          "oneOf": [
            {
              "$ref": "#/$defs/Value"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "code",
        "message"
      ]
    },
    "TaskErrorCode": {
      "description": "Error codes for task and step failures.\n\nEach variant encodes retryability semantics:\n\n* Always retried (transport): UNREACHABLE, TIMEOUT\n* Retried with onError policy: COMPONENT_FAILED, RESOURCE_UNAVAILABLE\n* Never retried: all others\n\nCases:\n* `UNSPECIFIED`: Default value; should not be used explicitly.\n* `TIMEOUT`: The task exceeded its execution deadline or heartbeat timeout.\nAlways retried (transport-level).\n* `INVALID_INPUT`: The component rejected its input (e.g., missing required fields, schema\nvalidation failure). Not retriable without correcting the input.\n* `COMPONENT_FAILED`: The component executed but returned a business-logic failure (e.g., an API\ncall returned an error, a transformation produced an invalid result).\nRetriable with onError: retry policy.\n* `CANCELLED`: The task was explicitly cancelled by the orchestrator (e.g., run\ncancellation, step timeout policy). Not retriable.\n* `UNREACHABLE`: The worker or component could not be reached (e.g., worker disconnected,\nsubprocess crash, network timeout). The component never executed.\nAlways retried (transport-level).\n* `COMPONENT_NOT_FOUND`: The requested component does not exist on the worker (e.g., unknown\ncomponent name, misconfigured route). Not retriable without fixing the\nworkflow or routing configuration.\n* `RESOURCE_UNAVAILABLE`: A resource required by the component was not available during execution\n(e.g., external API down, database connection failed).\nRetriable with onError: retry policy.\n* `EXPRESSION_FAILURE`: The orchestrator failed to resolve a value expression (e.g., $step\nreference to unknown step, path to nonexistent field).\nNot retriable — the flow definition must be corrected.\n* `ORCHESTRATOR_ERROR`: Catch-all for unexpected orchestrator errors (e.g., internal state\ncorruption, serialization failure). Not retriable.\n* `WORKER_ERROR`: Catch-all for unexpected worker/SDK errors (e.g., serialization failure,\npanic, protocol violation). Not retriable.",
      "type": "string",
      "enum": [
        "UNSPECIFIED",
        "TIMEOUT",
        "INVALID_INPUT",
        "COMPONENT_FAILED",
        "CANCELLED",
        "UNREACHABLE",
        "COMPONENT_NOT_FOUND",
        "RESOURCE_UNAVAILABLE",
        "EXPRESSION_FAILURE",
        "ORCHESTRATOR_ERROR",
        "WORKER_ERROR"
      ]
    },
    "MockPlugin": {
      "description": "A mock plugin that can be used to test various things in the plugin protocol.",
      "type": "object",
      "properties": {
        "components": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/MockComponent"
          }
        },
        "type": {
          "type": "string",
          "const": "mock",
          "default": "mock"
        }
      },
      "required": [
        "components",
        "type"
      ]
    },
    "McpPluginConfig": {
      "type": "object",
      "properties": {
        "command": {
          "type": "string"
        },
        "args": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "env": {
          "description": "Environment variables to pass to the MCP server process.\nValues can contain environment variable references like ${HOME} or ${USER:-default}.",
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "type": {
          "type": "string",
          "const": "mcp",
          "default": "mcp"
        }
      },
      "required": [
        "command",
        "args",
        "type"
      ]
    },
    "GrpcPluginConfig": {
      "description": "Configuration for a gRPC transport plugin.\n\nWhen instantiated, creates a [`StepflowQueuePlugin`] backed by an in-memory\ntask queue. During `ensure_initialized`, registers its queue with the\norchestrator's [`StepflowGrpcServer`] and optionally spawns a worker\nsubprocess.\n\n# Config example\n\n```yaml\nplugins:\n  python:\n    type: grpc\n    command: uv\n    args: [\"--project\", \"../sdks/python/stepflow-py\", \"run\", \"stepflow_py\", \"--grpc\"]\n    queueName: python\n```",
      "type": "object",
      "properties": {
        "command": {
          "description": "Command to launch the worker subprocess.\nIf not set, the plugin expects an external worker to connect.",
          "type": [
            "string",
            "null"
          ]
        },
        "args": {
          "description": "Arguments for the worker subprocess command.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "env": {
          "description": "Environment variables for the worker subprocess.",
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "queueName": {
          "description": "Queue name the worker uses to receive tasks. Required — must match\n`STEPFLOW_QUEUE_NAME` in the worker's environment.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "queueTimeoutSecs": {
          "description": "Maximum time (in seconds) a task can wait in the queue for a\nworker to send its first heartbeat. If no worker picks up the task\nwithin this window, it is treated as failed. Must be greater than 0.\n\nDefaults to 30 seconds.",
          "type": "integer",
          "format": "uint64",
          "minimum": 1,
          "default": 30
        },
        "executionTimeoutSecs": {
          "description": "Maximum time (in seconds) from first heartbeat to `CompleteTask`.\nIf the worker does not complete within this window, the task is\ntreated as failed. Heartbeat-based crash detection (5s timeout)\nprovides faster detection of hard worker crashes.\n\nDefaults to `null` (no execution timeout — relies on heartbeat\ncrash detection only).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0,
          "default": null
        },
        "type": {
          "type": "string",
          "const": "grpc",
          "default": "grpc"
        }
      },
      "required": [
        "type"
      ]
    },
    "NatsPluginConfig": {
      "description": "Configuration for a NATS JetStream transport plugin.\n\nEach route (or the plugin default) maps to a JetStream **stream** with\nWorkQueue retention. The stream is the unit of isolation — one per\nworker pool. Within each stream, tasks are published to a fixed\ninternal subject (`tasks`).\n\n# Config example\n\n```yaml\nplugins:\n  nats:\n    type: nats\n    url: \"nats://localhost:4222\"\n    stream: PYTHON_TASKS\n    consumer: python-workers\n    queueTimeoutSecs: 30\n\nroutes:\n  \"/python/foo\":\n    - plugin: nats\n      stream: FOO_TASKS          # separate worker pool\n  \"/python\":\n    - plugin: nats               # uses default stream\n```",
      "type": "object",
      "properties": {
        "url": {
          "description": "NATS server URL (e.g., \"nats://localhost:4222\").",
          "type": "string"
        },
        "stream": {
          "description": "Default JetStream stream name. Can be overridden per-route via\nthe `stream` route param. At least one of plugin-level or\nroute-level `stream` must be set.",
          "type": [
            "string",
            "null"
          ]
        },
        "consumer": {
          "description": "Durable consumer name for NATS JetStream. Required — workers use\nthis to create/resume a durable pull consumer within the stream.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "command": {
          "description": "Command to launch the worker subprocess.\nIf not set, the plugin expects an external worker to connect.",
          "type": [
            "string",
            "null"
          ]
        },
        "args": {
          "description": "Arguments for the worker subprocess command.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "env": {
          "description": "Environment variables for the worker subprocess.",
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "queueTimeoutSecs": {
          "description": "Maximum time (in seconds) a task can wait in the queue for a\nworker to send its first heartbeat. Defaults to 30 seconds.",
          "type": "integer",
          "format": "uint64",
          "minimum": 1,
          "default": 30
        },
        "executionTimeoutSecs": {
          "description": "Maximum time (in seconds) from first heartbeat to `CompleteTask`.\nDefaults to `null` (no execution timeout).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0,
          "default": null
        },
        "type": {
          "type": "string",
          "const": "nats",
          "default": "nats"
        }
      },
      "required": [
        "url",
        "type"
      ]
    },
    "RouteRule": {
      "description": "A single routing rule mapping a prefix to a plugin.",
      "type": "object",
      "properties": {
        "conditions": {
          "description": "Optional input conditions that must match for this rule to apply.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/InputCondition"
          }
        },
        "componentAllow": {
          "description": "Optional component allowlist — only these component IDs are allowed.\n\nIf omitted, all components are allowed.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        },
        "componentDeny": {
          "description": "Optional component denylist — these component IDs are blocked.\n\nIf omitted, no components are blocked.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        },
        "plugin": {
          "description": "Plugin name to route to.",
          "type": "string"
        }
      },
      "required": [
        "plugin"
      ],
      "additionalProperties": true
    },
    "InputCondition": {
      "description": "JSON path condition for matching specific parts of input data",
      "type": "object",
      "properties": {
        "path": {
          "description": "JSON path expression (e.g., \"$.model\", \"$.config.temperature\")",
          "$ref": "#/$defs/JsonPath"
        },
        "value": {
          "description": "Value to match against (equality comparison)",
          "$ref": "#/$defs/Value"
        }
      },
      "required": [
        "path",
        "value"
      ]
    },
    "JsonPath": {
      "description": "JSON path expression to apply to the referenced value.",
      "type": "string"
    },
    "StorageConfig": {
      "description": "Storage configuration supporting both simple and expanded forms.\n\n# Simple form (all stores share one backend)\n```yaml\nstorageConfig:\n  type: sqlite\n  databaseUrl: \"sqlite:workflow_state.db\"\n```\n\n# Expanded form (individual configs per store)\n```yaml\nstorageConfig:\n  metadata:\n    type: sqlite\n    databaseUrl: \"sqlite:workflow_state.db\"\n  blobs:\n    type: sqlite\n    databaseUrl: \"sqlite:workflow_state.db\"\n  journal:\n    type: inMemory\n```\n\nWhen multiple stores have identical configurations, they will share\na single backend instance (smart deduplication).",
      "anyOf": [
        {
          "title": "ExpandedStorageConfig",
          "description": "Expanded form: individual config per store",
          "type": "object",
          "properties": {
            "metadata": {
              "description": "Configuration for the metadata store",
              "$ref": "#/$defs/StoreConfig"
            },
            "blobs": {
              "description": "Configuration for the blob store (defaults to metadata config if not specified)",
              "default": null,
              "oneOf": [
                {
                  "$ref": "#/$defs/StoreConfig"
                },
                {
                  "type": "null"
                }
              ]
            },
            "journal": {
              "description": "Configuration for the execution journal (defaults to metadata config if not specified)",
              "default": null,
              "oneOf": [
                {
                  "$ref": "#/$defs/StoreConfig"
                },
                {
                  "type": "null"
                }
              ]
            }
          },
          "required": [
            "metadata"
          ]
        },
        {
          "title": "SimpleStorageConfig",
          "description": "Simple form: all stores share one backend",
          "$ref": "#/$defs/StoreConfig"
        }
      ]
    },
    "StoreConfig": {
      "description": "Configuration for a single storage backend.\n\nEach variant documents which store types it supports:\n- **metadata**: Flow and run metadata storage\n- **blobs**: Content-addressable blob storage\n- **journal**: Execution journal for recovery",
      "oneOf": [
        {
          "$ref": "#/$defs/InMemoryStore"
        },
        {
          "$ref": "#/$defs/SqliteStore"
        },
        {
          "$ref": "#/$defs/PostgresStore"
        },
        {
          "$ref": "#/$defs/FilesystemStore"
        }
      ],
      "discriminator": {
        "propertyName": "type",
        "mapping": {
          "inMemory": "#/$defs/InMemoryStore",
          "sqlite": "#/$defs/SqliteStore",
          "postgres": "#/$defs/PostgresStore",
          "filesystem": "#/$defs/FilesystemStore"
        }
      }
    },
    "SqlStateStoreConfig": {
      "description": "Configuration for SqlStateStore.\n\nThe dialect (SQLite vs PostgreSQL) is detected from the `database_url` prefix.",
      "type": "object",
      "properties": {
        "databaseUrl": {
          "type": "string"
        },
        "maxConnections": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "default": 10
        },
        "autoMigrate": {
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "databaseUrl"
      ]
    },
    "FilesystemBlobStoreConfig": {
      "description": "Configuration for the filesystem blob store.",
      "type": "object",
      "properties": {
        "directory": {
          "description": "Directory path for storing blobs. If not specified, a temporary directory is used.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "LeaseManagerConfig": {
      "description": "Configuration for the lease manager used in distributed deployments.\n\nThe lease manager handles run ownership in multi-orchestrator scenarios,\nensuring only one orchestrator executes a given run at a time.",
      "oneOf": [
        {
          "$ref": "#/$defs/NoOpLeaseManager"
        },
        {
          "$ref": "#/$defs/EtcdLeaseManager"
        }
      ],
      "discriminator": {
        "propertyName": "type",
        "mapping": {
          "noOp": "#/$defs/NoOpLeaseManager",
          "etcd": "#/$defs/EtcdLeaseManager"
        }
      }
    },
    "EtcdLeaseManagerConfig": {
      "description": "Configuration for the etcd lease manager.",
      "type": "object",
      "properties": {
        "endpoints": {
          "description": "etcd endpoints (e.g., `[\"http://localhost:2379\"]`).",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "key_prefix": {
          "description": "Key prefix for all stepflow lease keys.",
          "type": "string",
          "default": "/stepflow/leases"
        }
      },
      "required": [
        "endpoints"
      ]
    },
    "RecoveryConfig": {
      "description": "Configuration for run recovery and orphan claiming.\n\nControls how the orchestrator handles interrupted runs on startup\nand during execution.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Whether to enable periodic orphan claiming during execution.\n\nWhen enabled, the orchestrator will periodically check for orphaned\nruns (from crashed orchestrators) and claim them for execution.\nDefault: true",
          "type": "boolean",
          "default": true
        },
        "checkIntervalSecs": {
          "description": "Interval in seconds between orphan check attempts.\n\nOnly used when `enabled` is true. Lower values mean faster recovery\nbut more overhead. Default: 30 seconds.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 30
        },
        "maxStartupRecovery": {
          "description": "Maximum number of runs to recover on startup.\n\nLimits how many interrupted runs are recovered when the orchestrator\nstarts. Set to 0 to disable startup recovery. Default: 100.",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 100
        },
        "maxClaimsPerCheck": {
          "description": "Maximum number of orphaned runs to claim per check interval.\n\nLimits how many runs are claimed in each periodic check to avoid\noverwhelming a single orchestrator. Default: 10.",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 10
        },
        "leaseTtlSecs": {
          "description": "TTL in seconds for the orchestrator lease and heartbeats.\n\nThe heartbeat interval is automatically set to `lease_ttl_secs / 3`.\nIf an orchestrator stops sending heartbeats, its lease expires after this\nduration and its runs become eligible for recovery. Default: 30 seconds.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 30
        },
        "checkpointInterval": {
          "description": "Number of journal entries between checkpoints.\n\nThe executor periodically serializes execution state so that recovery\nonly needs to replay events after the checkpoint instead of from the\nbeginning. Set to 0 to disable. Default: 1000.",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 1000
        }
      }
    },
    "BlobApiConfig": {
      "description": "Configuration for the Blob HTTP API.\n\nThis controls whether the orchestrator serves blob API endpoints and what URL\nworkers should use to access the blob API.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Whether the orchestrator serves blob API endpoints.\n\nSet to `false` when running a separate blob service.\nDefault: `true`",
          "type": "boolean",
          "default": true
        },
        "url": {
          "description": "URL workers use to access the blob API.\n\nIf not set, defaults to `http://localhost:{port}/api/v1/blobs` where `{port}`\nis the server's bound port.\n\nThis value should be the base blobs endpoint URL. Workers will:\n- `POST {url}` to create blobs\n- `GET {url}/{blob_id}` to fetch blobs\n\nExamples:\n- Local dev: omit (auto-detected)\n- K8s with orchestrator blobs: `http://orchestrator-service/api/v1/blobs`\n- K8s with separate blob service: `http://blob-service/api/v1/blobs`",
          "type": [
            "string",
            "null"
          ]
        },
        "blobThreshold": {
          "description": "Byte size threshold for automatic blobification of component inputs/outputs.\n\nWhen a top-level field in a component's input or output exceeds this size\n(in bytes of JSON serialization), it is automatically stored as a blob and\nreplaced with a `$blob` reference.\n\nSet to `0` to disable automatic blobification.\nDefault: 1 MB (1048576 bytes)",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        }
      }
    },
    "RetryConfig": {
      "description": "Orchestrator-level retry configuration.\n\nControls backoff delays for all retries and the retry limit for transport\nerrors (subprocess crashes, network timeouts, connection refused).\n\nComponent error retry limits are configured per-step via\n`onError: { action: retry, maxRetries }` and share this backoff config.",
      "type": "object",
      "properties": {
        "transportMaxRetries": {
          "description": "Maximum number of retries for transport errors (default: 3).\n\nTransport errors are infrastructure-level failures — subprocess crashes,\nnetwork timeouts, connection refused — where the component never ran or\ndidn't complete.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "default": 3
        },
        "backoff": {
          "description": "Backoff strategy for all retry delays (default: fibonacci with 1s min, 10s max).\n\nThis backoff applies to both transport error retries and component error\nretries. The same delay progression is used regardless of retry reason.",
          "default": {
            "type": "fibonacci",
            "minDelayMs": 1000,
            "maxDelayMs": 10000
          },
          "oneOf": [
            {
              "$ref": "#/$defs/BackoffConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "BackoffConfig": {
      "description": "Backoff strategy for retry delays.",
      "oneOf": [
        {
          "$ref": "#/$defs/BackoffConfigConstant"
        },
        {
          "$ref": "#/$defs/BackoffConfigExponential"
        },
        {
          "$ref": "#/$defs/BackoffConfigFibonacci"
        }
      ],
      "discriminator": {
        "propertyName": "type",
        "mapping": {
          "constant": "#/$defs/BackoffConfigConstant",
          "exponential": "#/$defs/BackoffConfigExponential",
          "fibonacci": "#/$defs/BackoffConfigFibonacci"
        }
      }
    },
    "BuiltinPluginConfig": {
      "title": "BuiltinPluginConfig",
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "builtin",
          "default": "builtin"
        }
      },
      "required": [
        "type"
      ]
    },
    "InMemoryStore": {
      "title": "InMemoryStore",
      "description": "In-memory storage (default, for testing and demos).\n\n**Supported stores**: metadata, blobs, journal\n\nData is not persisted across restarts. Useful for development,\ntesting, and demos where persistence is not required.",
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "inMemory",
          "default": "inMemory"
        }
      },
      "required": [
        "type"
      ]
    },
    "SqliteStore": {
      "title": "SqliteStore",
      "description": "SQLite-based persistent storage.\n\n**Supported stores**: metadata, blobs, journal\n\nProvides durable storage with automatic schema migrations.\nSuitable for single-instance deployments and development.",
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "sqlite",
          "default": "sqlite"
        }
      },
      "$ref": "#/$defs/SqlStateStoreConfig",
      "required": [
        "type"
      ]
    },
    "PostgresStore": {
      "title": "PostgresStore",
      "description": "PostgreSQL-based persistent storage.\n\n**Supported stores**: metadata, blobs, journal\n\nProvides durable storage with automatic schema migrations.\nSuitable for multi-instance and production deployments.",
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "postgres",
          "default": "postgres"
        }
      },
      "$ref": "#/$defs/SqlStateStoreConfig",
      "required": [
        "type"
      ]
    },
    "FilesystemStore": {
      "title": "FilesystemStore",
      "description": "Filesystem-based blob storage.\n\n**Supported stores**: blobs only\n\nStores blobs as JSON files in a directory. If no directory is specified,\na temporary directory is created and cleaned up when the store is dropped.\nSuitable for local development and single-instance deployments.",
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "filesystem",
          "default": "filesystem"
        }
      },
      "$ref": "#/$defs/FilesystemBlobStoreConfig",
      "required": [
        "type"
      ]
    },
    "NoOpLeaseManager": {
      "title": "NoOpLeaseManager",
      "description": "No-op lease management (single orchestrator mode).\n\nThis is the default for single-node deployments where there's no need\nfor distributed coordination. All lease requests succeed immediately.",
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "noOp",
          "default": "noOp"
        }
      },
      "required": [
        "type"
      ]
    },
    "EtcdLeaseManager": {
      "title": "EtcdLeaseManager",
      "description": "etcd-backed lease management for distributed deployments.\n\nUses etcd v3 for distributed coordination with one etcd lease per\norchestrator. Provides automatic cleanup on crash, push-based orphan\ndetection, and efficient `release_all` via lease revocation.",
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "etcd",
          "default": "etcd"
        }
      },
      "$ref": "#/$defs/EtcdLeaseManagerConfig",
      "required": [
        "type"
      ]
    },
    "BackoffConfigConstant": {
      "title": "BackoffConfigConstant",
      "description": "Fixed delay between retries.",
      "type": "object",
      "properties": {
        "delayMs": {
          "description": "Delay in milliseconds (default: 1000).",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 1000
        },
        "type": {
          "type": "string",
          "const": "constant",
          "default": "constant"
        }
      },
      "required": [
        "type"
      ]
    },
    "BackoffConfigExponential": {
      "title": "BackoffConfigExponential",
      "description": "Exponential backoff (delay doubles each attempt by default).",
      "type": "object",
      "properties": {
        "minDelayMs": {
          "description": "Starting delay in milliseconds (default: 1000).",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 1000
        },
        "maxDelayMs": {
          "description": "Maximum delay cap in milliseconds (default: 10000).",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 10000
        },
        "factor": {
          "description": "Multiplier per attempt (default: 2.0).",
          "type": "number",
          "format": "float",
          "default": 2.0
        },
        "type": {
          "type": "string",
          "const": "exponential",
          "default": "exponential"
        }
      },
      "required": [
        "type"
      ]
    },
    "BackoffConfigFibonacci": {
      "title": "BackoffConfigFibonacci",
      "description": "Fibonacci backoff (delay follows the Fibonacci sequence).",
      "type": "object",
      "properties": {
        "minDelayMs": {
          "description": "Starting delay in milliseconds (default: 1000).",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 1000
        },
        "maxDelayMs": {
          "description": "Maximum delay cap in milliseconds (default: 10000).",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 10000
        },
        "type": {
          "type": "string",
          "const": "fibonacci",
          "default": "fibonacci"
        }
      },
      "required": [
        "type"
      ]
    }
  }
}
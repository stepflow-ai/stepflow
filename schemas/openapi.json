{
  "openapi": "3.1.0",
  "info": {
    "title": "Stepflow API",
    "description": "API for Stepflow flows and runs",
    "license": {
      "name": "Apache-2.0",
      "url": "https://www.apache.org/licenses/LICENSE-2.0"
    },
    "version": "0.9.0"
  },
  "servers": [
    {
      "url": "http://localhost:7840/api/v1",
      "description": "Default development server (port 7840)"
    }
  ],
  "paths": {
    "/health": {
      "get": {
        "tags": [
          "Health"
        ],
        "summary": "Check service health",
        "description": "Returns the current health status of the service. Use the `error` query parameter to trigger test error responses.",
        "operationId": "healthCheck",
        "parameters": [
          {
            "in": "query",
            "name": "error",
            "description": "Trigger an error for testing error response format",
            "schema": {
              "description": "Trigger an error for testing error response format",
              "type": [
                "string",
                "null"
              ]
            },
            "style": "form"
          }
        ],
        "responses": {
          "200": {
            "description": "Health check response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HealthResponse"
                }
              }
            }
          },
          "400": {
            "description": "Error triggered via query parameter",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/blobs": {
      "post": {
        "tags": [
          "Blob"
        ],
        "summary": "Store a blob",
        "description": "Store a blob and return its content-based ID. Supports two content types: `application/json` (JSON body with `data`, `blobType`, and optional `filename` fields) and `application/octet-stream` (raw binary body, use `X-Blob-Filename` header for filename).",
        "operationId": "storeBlob",
        "requestBody": {
          "description": "Blob data to store",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/StoreBlobRequest"
              }
            },
            "application/octet-stream": {
              "schema": {
                "type": "string",
                "format": "binary"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Blob stored successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StoreBlobResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid request body",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/blobs/{blob_id}": {
      "get": {
        "tags": [
          "Blob"
        ],
        "summary": "Get a blob by ID",
        "description": "Retrieve a blob by its content-based ID. Supports content negotiation via `Accept` header: `application/json` (default) returns JSON with data, blobType, blobId, and filename; `application/octet-stream` returns raw bytes.",
        "operationId": "getBlob",
        "parameters": [
          {
            "in": "path",
            "name": "blob_id",
            "description": "The blob's content-based hash ID",
            "required": true,
            "schema": {
              "description": "The blob's content-based hash ID",
              "$ref": "#/components/schemas/BlobId"
            },
            "style": "simple"
          }
        ],
        "responses": {
          "200": {
            "description": "Blob data",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GetBlobResponse"
                }
              }
            }
          },
          "404": {
            "description": "Blob not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/components": {
      "get": {
        "tags": [
          "Component"
        ],
        "summary": "List all available components",
        "description": "List all available components from registered plugins.",
        "operationId": "listComponents",
        "parameters": [
          {
            "in": "query",
            "name": "includeSchemas",
            "description": "Whether to include schemas in the response (default: true)",
            "schema": {
              "description": "Whether to include schemas in the response (default: true)",
              "type": "boolean",
              "default": true
            },
            "style": "form"
          }
        ],
        "responses": {
          "200": {
            "description": "Response for listing components",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ListComponentsResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid query parameters",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/runs": {
      "get": {
        "tags": [
          "Run"
        ],
        "summary": "List runs with optional filtering",
        "description": "List executions with optional status, flow name, and hierarchy filtering.",
        "operationId": "listRuns",
        "parameters": [
          {
            "in": "query",
            "name": "status",
            "description": "Filter by execution status",
            "schema": {
              "description": "Filter by execution status",
              "oneOf": [
                {
                  "$ref": "#/components/schemas/ExecutionStatus"
                },
                {
                  "type": "null"
                }
              ]
            },
            "style": "form"
          },
          {
            "in": "query",
            "name": "flowName",
            "description": "Filter by flow name",
            "schema": {
              "description": "Filter by flow name",
              "type": [
                "string",
                "null"
              ]
            },
            "style": "form"
          },
          {
            "in": "query",
            "name": "rootRunId",
            "description": "Filter to runs under this root (includes the root itself)",
            "schema": {
              "description": "Filter to runs under this root (includes the root itself)",
              "type": [
                "string",
                "null"
              ],
              "format": "uuid"
            },
            "style": "form"
          },
          {
            "in": "query",
            "name": "parentRunId",
            "description": "Filter to direct children of this parent run",
            "schema": {
              "description": "Filter to direct children of this parent run",
              "type": [
                "string",
                "null"
              ],
              "format": "uuid"
            },
            "style": "form"
          },
          {
            "in": "query",
            "name": "rootsOnly",
            "description": "Filter to only root runs (runs where parent_run_id is None)",
            "schema": {
              "description": "Filter to only root runs (runs where parent_run_id is None)",
              "type": [
                "boolean",
                "null"
              ]
            },
            "style": "form"
          },
          {
            "in": "query",
            "name": "maxDepth",
            "description": "Maximum depth for hierarchy queries (0 = root only)",
            "schema": {
              "description": "Maximum depth for hierarchy queries (0 = root only)",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint32",
              "minimum": 0
            },
            "style": "form"
          },
          {
            "in": "query",
            "name": "limit",
            "description": "Maximum number of results to return",
            "schema": {
              "description": "Maximum number of results to return",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint",
              "minimum": 0
            },
            "style": "form"
          },
          {
            "in": "query",
            "name": "offset",
            "description": "Number of results to skip",
            "schema": {
              "description": "Number of results to skip",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint",
              "minimum": 0
            },
            "style": "form"
          }
        ],
        "responses": {
          "200": {
            "description": "Response for listing runs",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ListRunsResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid query parameters",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": [
          "Run"
        ],
        "summary": "Create and execute a flow run",
        "description": "Create and execute a flow by hash. Supports both single and batch execution. By default, returns immediately with 202 Accepted. Set `wait: true` to block until the run completes and return 200 OK with the result.",
        "operationId": "createRun",
        "requestBody": {
          "description": "Request to create/execute a flow.\n\nThe `input` field is always an array of input values:\n- Single-item array `[value]`: Executes one run with `value` as input\n- Multi-item array `[v1, v2, ...]`: Executes multiple runs (batch mode)\n\nThis design avoids ambiguity: to run a workflow with an array as input,\nwrap it in another array: `[[1, 2, 3]]` runs once with input `[1, 2, 3]`.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateRunRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "202": {
            "description": "Run created and executing asynchronously",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CreateRunResponse"
                }
              }
            }
          },
          "200": {
            "description": "Run completed (when wait=true)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CreateRunResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid request",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Flow not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/runs/{run_id}": {
      "get": {
        "tags": [
          "Run"
        ],
        "summary": "Get run details by ID",
        "description": "Returns the current run status and details. Use `wait=true` to long-poll until the run reaches a terminal state (completed, failed, or cancelled).",
        "operationId": "getRun",
        "parameters": [
          {
            "in": "path",
            "name": "run_id",
            "description": "Run ID (UUID)",
            "required": true,
            "schema": {
              "description": "Run ID (UUID)",
              "type": "string",
              "format": "uuid"
            },
            "style": "simple"
          },
          {
            "in": "query",
            "name": "wait",
            "description": "If true, wait for the run to reach a terminal state before responding.",
            "schema": {
              "description": "If true, wait for the run to reach a terminal state before responding.",
              "type": [
                "boolean",
                "null"
              ],
              "default": null
            },
            "style": "form"
          },
          {
            "in": "query",
            "name": "timeoutSecs",
            "description": "Maximum seconds to wait when wait=true (default 300). If the timeout elapses,\nreturns the current run status rather than an error.",
            "schema": {
              "description": "Maximum seconds to wait when wait=true (default 300). If the timeout elapses,\nreturns the current run status rather than an error.",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0,
              "default": null
            },
            "style": "form"
          }
        ],
        "responses": {
          "200": {
            "description": "Detailed flow run information including item details.\n\nFor completed runs, `item_details` contains per-item information including\ninputs and step statuses. For active runs, `item_details` may be `None`\n(query the owning orchestrator for live status).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunDetails"
                }
              }
            }
          },
          "404": {
            "description": "Run not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      },
      "delete": {
        "tags": [
          "Run"
        ],
        "summary": "Delete a completed execution",
        "description": "Delete a completed execution by ID. Running executions cannot be deleted.",
        "operationId": "deleteRun",
        "parameters": [
          {
            "in": "path",
            "name": "run_id",
            "description": "Run ID (UUID)",
            "required": true,
            "schema": {
              "description": "Run ID (UUID)",
              "type": "string",
              "format": "uuid"
            },
            "style": "simple"
          }
        ],
        "responses": {
          "200": {
            "description": "no content"
          },
          "404": {
            "description": "Run not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "Run is still running and cannot be deleted",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/runs/{run_id}/items": {
      "get": {
        "tags": [
          "Run"
        ],
        "summary": "Get all item results for a run",
        "description": "Returns results for all items in the run, ordered by item index. For single-item runs (item_count=1), returns a single item.",
        "operationId": "getRunItems",
        "parameters": [
          {
            "in": "path",
            "name": "run_id",
            "description": "Run ID (UUID)",
            "required": true,
            "schema": {
              "description": "Run ID (UUID)",
              "type": "string",
              "format": "uuid"
            },
            "style": "simple"
          }
        ],
        "responses": {
          "200": {
            "description": "Response for listing run items",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ListItemsResponse"
                }
              }
            }
          },
          "404": {
            "description": "Run not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/runs/{run_id}/flow": {
      "get": {
        "tags": [
          "Run"
        ],
        "summary": "Get the workflow definition for a run",
        "description": "Retrieve the workflow definition associated with a specific run.",
        "operationId": "getRunFlow",
        "parameters": [
          {
            "in": "path",
            "name": "run_id",
            "description": "Run ID (UUID)",
            "required": true,
            "schema": {
              "description": "Run ID (UUID)",
              "type": "string",
              "format": "uuid"
            },
            "style": "simple"
          }
        ],
        "responses": {
          "200": {
            "description": "Response containing a flow definition and its hash for run endpoints",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunFlowResponse"
                }
              }
            }
          },
          "404": {
            "description": "Run not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/runs/{run_id}/steps": {
      "get": {
        "tags": [
          "Run"
        ],
        "summary": "Get step-level execution details",
        "description": "Get step-level execution details for a specific run. Use `item_index` to get statuses for a specific item in batch runs. Without `item_index`, statuses are aggregated across all items.",
        "operationId": "getRunSteps",
        "parameters": [
          {
            "in": "path",
            "name": "run_id",
            "description": "Run ID (UUID)",
            "required": true,
            "schema": {
              "description": "Run ID (UUID)",
              "type": "string",
              "format": "uuid"
            },
            "style": "simple"
          },
          {
            "in": "query",
            "name": "itemIndex",
            "description": "Optional item index for multi-item (batch) runs.\nIf not specified, step statuses are aggregated across all items\nusing \"worst status\" precedence (Failed > Running > Runnable > Blocked > Skipped > Completed).",
            "schema": {
              "description": "Optional item index for multi-item (batch) runs.\nIf not specified, step statuses are aggregated across all items\nusing \"worst status\" precedence (Failed > Running > Runnable > Blocked > Skipped > Completed).",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint",
              "minimum": 0,
              "default": null
            },
            "style": "form"
          }
        ],
        "responses": {
          "200": {
            "description": "Response for listing step runs",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ListStepRunsResponse"
                }
              }
            }
          },
          "404": {
            "description": "Run not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/runs/{run_id}/cancel": {
      "post": {
        "tags": [
          "Run"
        ],
        "summary": "Cancel a running execution",
        "description": "Cancel a running execution by ID.",
        "operationId": "cancelRun",
        "parameters": [
          {
            "in": "path",
            "name": "run_id",
            "description": "Run ID (UUID)",
            "required": true,
            "schema": {
              "description": "Run ID (UUID)",
              "type": "string",
              "format": "uuid"
            },
            "style": "simple"
          }
        ],
        "responses": {
          "200": {
            "description": "Summary information about a flow run.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunSummary"
                }
              }
            }
          },
          "404": {
            "description": "Run not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "Run cannot be cancelled (already completed)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/flows": {
      "post": {
        "tags": [
          "Flow"
        ],
        "summary": "Store a flow",
        "description": "Store a flow and return its content-based hash ID.",
        "operationId": "storeFlow",
        "requestBody": {
          "description": "Request to store a flow",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/StoreFlowRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Response when a flow is stored",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StoreFlowResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid flow definition",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/flows/{flow_id}": {
      "get": {
        "tags": [
          "Flow"
        ],
        "summary": "Get a flow by ID",
        "description": "Retrieve a flow definition by its content-based hash ID.",
        "operationId": "getFlow",
        "parameters": [
          {
            "in": "path",
            "name": "flow_id",
            "description": "The flow's content-based hash ID",
            "required": true,
            "schema": {
              "description": "The flow's content-based hash ID",
              "$ref": "#/components/schemas/BlobId"
            },
            "style": "simple"
          }
        ],
        "responses": {
          "200": {
            "description": "Response containing a flow definition and its ID",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FlowResponse"
                }
              }
            }
          },
          "404": {
            "description": "Flow not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      },
      "delete": {
        "tags": [
          "Flow"
        ],
        "summary": "Delete a flow by ID",
        "description": "Delete a flow by its content-based hash ID.",
        "operationId": "deleteFlow",
        "parameters": [
          {
            "in": "path",
            "name": "flow_id",
            "description": "The flow's content-based hash ID",
            "required": true,
            "schema": {
              "description": "The flow's content-based hash ID",
              "$ref": "#/components/schemas/BlobId"
            },
            "style": "simple"
          }
        ],
        "responses": {
          "200": {
            "description": "no content"
          },
          "404": {
            "description": "Flow not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "OnErrorRetry": {
        "title": "OnErrorRetry",
        "description": "If the step fails, retry it.",
        "type": "object",
        "properties": {
          "action": {
            "type": "string",
            "const": "retry",
            "default": "retry"
          }
        },
        "required": [
          "action"
        ]
      },
      "HealthResponse": {
        "description": "Health check response",
        "type": "object",
        "properties": {
          "status": {
            "description": "Service status",
            "type": "string"
          },
          "timestamp": {
            "description": "Timestamp when health was checked (RFC3339 format)",
            "type": "string"
          },
          "version": {
            "description": "Service version",
            "type": "string"
          }
        },
        "required": [
          "status",
          "timestamp",
          "version"
        ]
      },
      "ErrorResponse": {
        "description": "Error response structure.\n\nServer handlers should return this, but usually it is better to create it\nby returning an `error_stack::Report<ServerError>` and using the automatic\nconversion to `ErrorResponse`.\n\nOther `error_stack::Report` types will automatically convert to internal errors.",
        "type": "object",
        "properties": {
          "code": {
            "description": "HTTP status code (e.g., 400, 404, 500)",
            "type": "integer",
            "format": "uint16",
            "minimum": 0,
            "maximum": 65535
          },
          "message": {
            "description": "Human-readable error message",
            "type": "string"
          },
          "stack": {
            "description": "Detailed error stack for debugging",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ErrorStackEntry"
            }
          }
        },
        "required": [
          "code",
          "message"
        ]
      },
      "ErrorStackEntry": {
        "description": "A single entry in an error stack for detailed error reporting",
        "type": "object",
        "properties": {
          "error": {
            "description": "The error message",
            "type": "string"
          },
          "attachments": {
            "description": "Additional context attached to this error",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "backtrace": {
            "description": "Backtrace if available",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "error"
        ]
      },
      "StoreBlobResponse": {
        "description": "Response when a blob is stored",
        "type": "object",
        "properties": {
          "blobId": {
            "description": "The content-based blob ID (SHA-256 hash)",
            "allOf": [
              {
                "$ref": "#/components/schemas/BlobId"
              }
            ]
          },
          "filename": {
            "description": "The filename if one was provided",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "blobId"
        ]
      },
      "BlobId": {
        "description": "A SHA-256 hash of the blob content, represented as a hexadecimal string.",
        "type": "string"
      },
      "StoreBlobRequest": {
        "description": "Request to store a blob (JSON mode)",
        "type": "object",
        "properties": {
          "data": {
            "description": "The JSON data to store"
          },
          "blobType": {
            "description": "The type of blob (data or flow). Defaults to \"data\".",
            "default": "data",
            "allOf": [
              {
                "$ref": "#/components/schemas/BlobType"
              }
            ]
          },
          "filename": {
            "description": "Optional filename to associate with the blob.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "data"
        ]
      },
      "BlobType": {
        "description": "Type of blob stored in the blob store.\n\nCases:\n* `flow`: A workflow/flow definition\n* `data`: Generic data blob\n* `binary`: Raw binary data (base64-encoded in JSON transport)",
        "type": "string",
        "enum": [
          "flow",
          "data",
          "binary"
        ]
      },
      "OnErrorDefault": {
        "title": "OnErrorDefault",
        "description": "If the step fails, use the `defaultValue` instead.\nIf `defaultValue` is not specified, the step returns null.\nThe default value must be a literal JSON value (not an expression).\nFor dynamic defaults, use `$coalesce` in the consuming expression instead.",
        "type": "object",
        "properties": {
          "defaultValue": true,
          "action": {
            "type": "string",
            "const": "useDefault",
            "default": "useDefault"
          }
        },
        "required": [
          "action"
        ]
      },
      "GetBlobResponse": {
        "description": "Response when retrieving a blob (JSON mode)",
        "type": "object",
        "properties": {
          "data": {
            "description": "The blob data"
          },
          "blobType": {
            "description": "The blob type",
            "allOf": [
              {
                "$ref": "#/components/schemas/BlobType"
              }
            ]
          },
          "blobId": {
            "description": "The blob ID (for confirmation)",
            "allOf": [
              {
                "$ref": "#/components/schemas/BlobId"
              }
            ]
          },
          "filename": {
            "description": "The filename if one was set",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "data",
          "blobType",
          "blobId"
        ]
      },
      "OnErrorFail": {
        "title": "OnErrorFail",
        "description": "If the step fails, the flow will fail.",
        "type": "object",
        "properties": {
          "action": {
            "type": "string",
            "const": "fail",
            "default": "fail"
          }
        },
        "required": [
          "action"
        ]
      },
      "ListComponentsResponse": {
        "description": "Response for listing components",
        "type": "object",
        "properties": {
          "components": {
            "description": "List of available components",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ComponentInfo"
            }
          }
        },
        "required": [
          "components"
        ]
      },
      "ComponentInfo": {
        "type": "object",
        "properties": {
          "component": {
            "description": "The component ID.",
            "allOf": [
              {
                "$ref": "#/components/schemas/Component"
              }
            ]
          },
          "description": {
            "description": "Optional description of the component.",
            "type": [
              "string",
              "null"
            ]
          },
          "input_schema": {
            "description": "The input schema for the component.\n\nCan be any valid JSON schema (object, primitive, array, etc.).",
            "oneOf": [
              {
                "$ref": "#/components/schemas/Schema"
              },
              {
                "type": "null"
              }
            ]
          },
          "output_schema": {
            "description": "The output schema for the component.\n\nCan be any valid JSON schema (object, primitive, array, etc.).",
            "oneOf": [
              {
                "$ref": "#/components/schemas/Schema"
              },
              {
                "type": "null"
              }
            ]
          }
        },
        "required": [
          "component"
        ]
      },
      "Component": {
        "description": "Identifies a specific plugin and atomic functionality to execute. Use component name for builtins (e.g., 'eval') or path format for plugins (e.g., '/python/udf').",
        "type": "string",
        "examples": [
          "/builtin/eval",
          "/mcpfs/list_files",
          "/python/udf"
        ]
      },
      "Schema": {
        "description": "A valid JSON Schema object.",
        "type": "object",
        "additionalProperties": true
      },
      "FlowResponse": {
        "description": "Response containing a flow definition and its ID",
        "type": "object",
        "properties": {
          "flow": {
            "description": "The flow definition",
            "allOf": [
              {
                "$ref": "#/components/schemas/Flow"
              }
            ]
          },
          "flowId": {
            "description": "The flow ID",
            "allOf": [
              {
                "$ref": "#/components/schemas/BlobId"
              }
            ]
          },
          "allExamples": {
            "description": "All available examples (includes both examples and test cases)",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ExampleInput"
            }
          }
        },
        "required": [
          "flow",
          "flowId",
          "allExamples"
        ]
      },
      "ExecutionStatus": {
        "description": "Status of a workflow execution\n\nCases:\n* `running`: Execution is currently running\n* `completed`: Execution completed successfully\n* `failed`: Execution failed with an error\n* `cancelled`: Execution was cancelled by user request\n* `paused`: Execution is paused (debug mode)\n* `recoveryFailed`: Execution failed during recovery after orchestrator restart",
        "type": "string",
        "enum": [
          "running",
          "completed",
          "failed",
          "cancelled",
          "paused",
          "recoveryFailed"
        ]
      },
      "ListRunsResponse": {
        "description": "Response for listing runs",
        "type": "object",
        "properties": {
          "runs": {
            "description": "List of run summaries",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RunSummary"
            }
          }
        },
        "required": [
          "runs"
        ]
      },
      "RunSummary": {
        "description": "Summary information about a flow run.",
        "type": "object",
        "properties": {
          "runId": {
            "type": "string",
            "format": "uuid"
          },
          "flowId": {
            "$ref": "#/components/schemas/BlobId"
          },
          "flowName": {
            "type": [
              "string",
              "null"
            ]
          },
          "status": {
            "$ref": "#/components/schemas/ExecutionStatus"
          },
          "items": {
            "description": "Statistics about items in this run.",
            "default": {
              "total": 0,
              "completed": 0,
              "running": 0,
              "failed": 0,
              "cancelled": 0
            },
            "allOf": [
              {
                "$ref": "#/components/schemas/ItemStatistics"
              }
            ]
          },
          "createdAt": {
            "type": "string",
            "format": "date-time"
          },
          "completedAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time"
          },
          "rootRunId": {
            "description": "Root run ID for this execution tree.\n\nFor top-level runs, this equals `run_id`.\nFor sub-flows, this is the original run that started the tree.",
            "type": "string",
            "format": "uuid"
          },
          "parentRunId": {
            "description": "Parent run ID if this is a sub-flow.\n\nNone for top-level runs, Some(parent_id) for sub-flows.",
            "type": [
              "string",
              "null"
            ],
            "format": "uuid"
          },
          "orchestratorId": {
            "description": "The orchestrator currently managing this run.\n\nNone means the run is orphaned (no orchestrator owns it).\nSet when the run is created and updated during recovery.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "runId",
          "flowId",
          "status",
          "createdAt",
          "rootRunId"
        ]
      },
      "ItemStatistics": {
        "description": "Statistics about items in a run.",
        "type": "object",
        "properties": {
          "total": {
            "description": "Total number of items in this run.",
            "type": "integer",
            "format": "uint",
            "minimum": 0
          },
          "completed": {
            "description": "Number of completed items.",
            "type": "integer",
            "format": "uint",
            "minimum": 0
          },
          "running": {
            "description": "Number of currently running items.",
            "type": "integer",
            "format": "uint",
            "minimum": 0
          },
          "failed": {
            "description": "Number of failed items.",
            "type": "integer",
            "format": "uint",
            "minimum": 0
          },
          "cancelled": {
            "description": "Number of cancelled items.",
            "type": "integer",
            "format": "uint",
            "minimum": 0
          }
        },
        "required": [
          "total",
          "completed",
          "running",
          "failed",
          "cancelled"
        ]
      },
      "CreateRunRequest": {
        "description": "Request to create/execute a flow.\n\nThe `input` field is always an array of input values:\n- Single-item array `[value]`: Executes one run with `value` as input\n- Multi-item array `[v1, v2, ...]`: Executes multiple runs (batch mode)\n\nThis design avoids ambiguity: to run a workflow with an array as input,\nwrap it in another array: `[[1, 2, 3]]` runs once with input `[1, 2, 3]`.",
        "type": "object",
        "properties": {
          "flowId": {
            "description": "The flow hash to execute",
            "allOf": [
              {
                "$ref": "#/components/schemas/BlobId"
              }
            ]
          },
          "input": {
            "description": "Input data for the flow - always an array (one element per run)",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Value"
            }
          },
          "overrides": {
            "description": "Optional workflow overrides to apply before execution",
            "allOf": [
              {
                "$ref": "#/components/schemas/WorkflowOverrides"
              }
            ]
          },
          "variables": {
            "description": "Optional variables to provide for variable references in the workflow",
            "type": "object",
            "additionalProperties": {
              "$ref": "#/components/schemas/Value"
            }
          },
          "maxConcurrency": {
            "description": "Maximum concurrency for batch execution (only used when input is an array)",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint",
            "minimum": 0
          },
          "wait": {
            "description": "If true, block until the run completes and return the result (200 OK).\nIf false (default), return immediately with status Running (202 Accepted).",
            "type": "boolean",
            "default": false
          },
          "timeoutSecs": {
            "description": "Maximum seconds to wait when wait=true (default 300). If the timeout elapses,\nreturns the current run status rather than an error.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "minimum": 0
          }
        },
        "required": [
          "flowId",
          "input"
        ]
      },
      "Value": {
        "description": "Any JSON value (object, array, string, number, boolean, or null)"
      },
      "WorkflowOverrides": {
        "description": "Workflow overrides that can be applied to modify step behavior at runtime.\n\nOverrides are keyed by step ID and contain merge patches or other transformation\nspecifications to modify step properties before execution.",
        "type": "object",
        "properties": {
          "steps": {
            "description": "Map of step ID to override specification",
            "type": "object",
            "additionalProperties": {
              "$ref": "#/components/schemas/StepOverride"
            }
          }
        },
        "required": [
          "steps"
        ]
      },
      "StepOverride": {
        "description": "Override specification for a single step.\n\nContains the override type (merge patch, json patch, etc.) and the value\nto apply. The type field uses `$type` to avoid collisions with step properties.",
        "type": "object",
        "properties": {
          "$type": {
            "description": "The type of override to apply. Defaults to \"merge_patch\" if not specified.",
            "default": "merge_patch",
            "allOf": [
              {
                "$ref": "#/components/schemas/OverrideType"
              }
            ]
          },
          "value": {
            "description": "The override value to apply, interpreted based on the override type."
          }
        },
        "required": [
          "value"
        ]
      },
      "OverrideType": {
        "description": "The type of override operation to perform.\n\nCases:\n* `merge_patch`: Apply a JSON Merge Patch (RFC 7396) to the step.\n\nThis is the default override type. The value should be a JSON object\nwhere null values indicate fields to remove and other values are merged\ninto the target step.\n* `json_patch`: Apply a JSON Patch (RFC 6902) to the step. (Future extension)\n\nThe value should be an array of JSON Patch operations.\nThis is reserved for future use.",
        "type": "string",
        "enum": [
          "merge_patch",
          "json_patch"
        ]
      },
      "CreateRunResponse": {
        "description": "Response for create run operations.\n\nShares the same base fields as `RunDetails` (GET /runs/{id}) via `RunSummary`,\nwith optional item results when `wait=true`.",
        "type": "object",
        "properties": {
          "runId": {
            "type": "string",
            "format": "uuid"
          },
          "flowId": {
            "$ref": "#/components/schemas/BlobId"
          },
          "flowName": {
            "type": [
              "string",
              "null"
            ]
          },
          "status": {
            "$ref": "#/components/schemas/ExecutionStatus"
          },
          "items": {
            "description": "Statistics about items in this run.",
            "default": {
              "total": 0,
              "completed": 0,
              "running": 0,
              "failed": 0,
              "cancelled": 0
            },
            "allOf": [
              {
                "$ref": "#/components/schemas/ItemStatistics"
              }
            ]
          },
          "createdAt": {
            "type": "string",
            "format": "date-time"
          },
          "completedAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time"
          },
          "rootRunId": {
            "description": "Root run ID for this execution tree.\n\nFor top-level runs, this equals `run_id`.\nFor sub-flows, this is the original run that started the tree.",
            "type": "string",
            "format": "uuid"
          },
          "parentRunId": {
            "description": "Parent run ID if this is a sub-flow.\n\nNone for top-level runs, Some(parent_id) for sub-flows.",
            "type": [
              "string",
              "null"
            ],
            "format": "uuid"
          },
          "orchestratorId": {
            "description": "The orchestrator currently managing this run.\n\nNone means the run is orphaned (no orchestrator owns it).\nSet when the run is created and updated during recovery.",
            "type": [
              "string",
              "null"
            ]
          },
          "results": {
            "description": "Item results, only populated when `wait=true` and the run has completed.\nEach entry contains the item's index, status, and result.",
            "type": [
              "array",
              "null"
            ],
            "items": {
              "$ref": "#/components/schemas/ItemResult"
            }
          }
        },
        "required": [
          "runId",
          "flowId",
          "status",
          "createdAt",
          "rootRunId"
        ]
      },
      "ItemResult": {
        "description": "Result for an individual item in a multi-item run.",
        "type": "object",
        "properties": {
          "itemIndex": {
            "description": "Index of this item in the input array (0-based).",
            "type": "integer",
            "format": "uint",
            "minimum": 0
          },
          "status": {
            "description": "Execution status of this item.",
            "allOf": [
              {
                "$ref": "#/components/schemas/ExecutionStatus"
              }
            ]
          },
          "result": {
            "description": "Result of this item, if completed.",
            "oneOf": [
              {
                "$ref": "#/components/schemas/FlowResult"
              },
              {
                "type": "null"
              }
            ]
          },
          "completedAt": {
            "description": "When this item completed (if completed).",
            "type": [
              "string",
              "null"
            ],
            "format": "date-time"
          }
        },
        "required": [
          "itemIndex",
          "status"
        ]
      },
      "FlowResult": {
        "oneOf": [
          {
            "$ref": "#/components/schemas/FlowResultSuccess"
          },
          {
            "$ref": "#/components/schemas/FlowResultFailed"
          }
        ],
        "discriminator": {
          "propertyName": "outcome",
          "mapping": {
            "success": "#/components/schemas/FlowResultSuccess",
            "failed": "#/components/schemas/FlowResultFailed"
          }
        }
      },
      "FlowResultSuccess": {
        "description": "The step execution was successful.",
        "type": "object",
        "properties": {
          "outcome": {
            "type": "string",
            "const": "success",
            "default": "success"
          },
          "result": {
            "$ref": "#/components/schemas/Value"
          }
        },
        "required": [
          "outcome",
          "result"
        ]
      },
      "FlowResultFailed": {
        "description": "The step failed with the given error.",
        "type": "object",
        "properties": {
          "outcome": {
            "type": "string",
            "const": "failed",
            "default": "failed"
          },
          "error": {
            "$ref": "#/components/schemas/FlowError"
          }
        },
        "required": [
          "outcome",
          "error"
        ]
      },
      "FlowError": {
        "description": "An error reported from within a flow or step.",
        "type": "object",
        "properties": {
          "code": {
            "type": "integer",
            "format": "int64"
          },
          "message": {
            "type": "string"
          },
          "data": {
            "oneOf": [
              {
                "$ref": "#/components/schemas/Value"
              },
              {
                "type": "null"
              }
            ]
          }
        },
        "required": [
          "code",
          "message"
        ]
      },
      "Diagnostic": {
        "description": "A diagnostic with error code, message, path, and metadata.\n\nCreated via the `diagnostic!` macro with optional builder methods:\n\n```ignore\ndiagnostic!(DiagnosticKind::DuplicateStepId, \"Duplicate step ID '{step_id}'\", { step_id })\n    .at(path)\n    .experimental()\n```\n\n## JSON Format\n\n```json\n{\n  \"kind\": \"duplicateStepId\",\n  \"code\": 3000,\n  \"level\": \"fatal\",\n  \"formatted\": \"Duplicate step ID 'foo'\",\n  \"data\": { \"stepId\": \"foo\" },\n  \"path\": \"$.steps[0]\",\n  \"experimental\": false\n}\n```",
        "type": "object",
        "properties": {
          "kind": {
            "description": "The diagnostic kind name (camelCase)",
            "type": "string"
          },
          "code": {
            "description": "Numeric error code",
            "type": "integer",
            "format": "uint16",
            "minimum": 0,
            "maximum": 65535
          },
          "level": {
            "description": "The severity level",
            "allOf": [
              {
                "$ref": "#/components/schemas/DiagnosticLevel"
              }
            ]
          },
          "formatted": {
            "description": "Human-readable formatted message",
            "type": "string"
          },
          "data": {
            "description": "Structured data with camelCase keys (null if no data)"
          },
          "path": {
            "description": "JSON path to the field with the issue",
            "allOf": [
              {
                "$ref": "#/components/schemas/Path"
              }
            ]
          },
          "experimental": {
            "description": "Whether this diagnostic is experimental (may have false positives)",
            "type": "boolean"
          }
        },
        "required": [
          "kind",
          "code",
          "level",
          "formatted"
        ]
      },
      "Path": {
        "description": "Path to a location in the workflow definition, serialized as a string",
        "type": "string"
      },
      "RunDetails": {
        "description": "Detailed flow run information including item details.\n\nFor completed runs, `item_details` contains per-item information including\ninputs and step statuses. For active runs, `item_details` may be `None`\n(query the owning orchestrator for live status).",
        "type": "object",
        "properties": {
          "runId": {
            "type": "string",
            "format": "uuid"
          },
          "flowId": {
            "$ref": "#/components/schemas/BlobId"
          },
          "flowName": {
            "type": [
              "string",
              "null"
            ]
          },
          "status": {
            "$ref": "#/components/schemas/ExecutionStatus"
          },
          "items": {
            "description": "Statistics about items in this run.",
            "default": {
              "total": 0,
              "completed": 0,
              "running": 0,
              "failed": 0,
              "cancelled": 0
            },
            "allOf": [
              {
                "$ref": "#/components/schemas/ItemStatistics"
              }
            ]
          },
          "createdAt": {
            "type": "string",
            "format": "date-time"
          },
          "completedAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time"
          },
          "rootRunId": {
            "description": "Root run ID for this execution tree.\n\nFor top-level runs, this equals `run_id`.\nFor sub-flows, this is the original run that started the tree.",
            "type": "string",
            "format": "uuid"
          },
          "parentRunId": {
            "description": "Parent run ID if this is a sub-flow.\n\nNone for top-level runs, Some(parent_id) for sub-flows.",
            "type": [
              "string",
              "null"
            ],
            "format": "uuid"
          },
          "orchestratorId": {
            "description": "The orchestrator currently managing this run.\n\nNone means the run is orphaned (no orchestrator owns it).\nSet when the run is created and updated during recovery.",
            "type": [
              "string",
              "null"
            ]
          },
          "itemDetails": {
            "description": "Item details with inputs and step statuses.\n- `None`: details not requested, or run is active (query executor)\n- `Some`: item-level details available",
            "type": [
              "array",
              "null"
            ],
            "items": {
              "$ref": "#/components/schemas/ItemDetails"
            }
          },
          "overrides": {
            "description": "Optional workflow overrides applied to this run (per-run, not per-item).",
            "oneOf": [
              {
                "$ref": "#/components/schemas/WorkflowOverrides"
              },
              {
                "type": "null"
              }
            ]
          }
        },
        "required": [
          "runId",
          "flowId",
          "status",
          "createdAt",
          "rootRunId"
        ]
      },
      "ItemDetails": {
        "description": "Detailed information for a single item in a run.\n\nIncludes the item's input, execution status, and step-level details.",
        "type": "object",
        "properties": {
          "itemIndex": {
            "description": "Index of this item in the input array (0-based).",
            "type": "integer",
            "format": "uint32",
            "minimum": 0
          },
          "input": {
            "description": "Input value for this item.",
            "allOf": [
              {
                "$ref": "#/components/schemas/Value"
              }
            ]
          },
          "status": {
            "description": "Execution status of this item.",
            "allOf": [
              {
                "$ref": "#/components/schemas/ExecutionStatus"
              }
            ]
          },
          "steps": {
            "description": "Step statuses for this item.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/StepStatusInfo"
            }
          },
          "completedAt": {
            "description": "When this item completed (if completed).",
            "type": [
              "string",
              "null"
            ],
            "format": "date-time"
          }
        },
        "required": [
          "itemIndex",
          "input",
          "status",
          "steps"
        ]
      },
      "StepStatusInfo": {
        "description": "Status information for a single step execution.",
        "type": "object",
        "properties": {
          "stepId": {
            "description": "Step name/identifier.",
            "type": "string"
          },
          "status": {
            "description": "Current status of the step.",
            "allOf": [
              {
                "$ref": "#/components/schemas/StepStatus"
              }
            ]
          }
        },
        "required": [
          "stepId",
          "status"
        ]
      },
      "StepStatus": {
        "description": "Status of an individual step within a workflow\n\nCases:\n* `blocked`: Step is waiting for dependencies to complete\n* `runnable`: Step is ready to be executed\n* `running`: Step is currently executing\n* `completed`: Step has been executed successfully\n* `skipped`: Step was skipped due to conditions\n* `failed`: Step failed with an error",
        "type": "string",
        "enum": [
          "blocked",
          "runnable",
          "running",
          "completed",
          "skipped",
          "failed"
        ]
      },
      "ListItemsResponse": {
        "description": "Response for listing run items",
        "type": "object",
        "properties": {
          "itemCount": {
            "description": "Total number of items in this run",
            "type": "integer",
            "format": "uint",
            "minimum": 0
          },
          "items": {
            "description": "Individual item results ordered by item index",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ItemResult"
            }
          }
        },
        "required": [
          "itemCount",
          "items"
        ]
      },
      "RunFlowResponse": {
        "description": "Response containing a flow definition and its hash for run endpoints",
        "type": "object",
        "properties": {
          "flow": {
            "description": "The flow definition",
            "allOf": [
              {
                "$ref": "#/components/schemas/Flow"
              }
            ]
          },
          "flowId": {
            "description": "The flow hash",
            "allOf": [
              {
                "$ref": "#/components/schemas/BlobId"
              }
            ]
          }
        },
        "required": [
          "flow",
          "flowId"
        ]
      },
      "Flow": {
        "description": "A workflow consisting of a sequence of steps and their outputs.\n\nA flow represents a complete workflow that can be executed. It contains:\n- A sequence of steps to execute\n- Named outputs that can reference step outputs\n\nFlows should not be cloned. They should generally be stored and passed as a\nreference or inside an `Arc`.",
        "type": "object",
        "properties": {
          "name": {
            "description": "The name of the flow.",
            "type": [
              "string",
              "null"
            ]
          },
          "description": {
            "description": "The description of the flow.",
            "type": [
              "string",
              "null"
            ]
          },
          "version": {
            "description": "The version of the flow.",
            "type": [
              "string",
              "null"
            ]
          },
          "schemas": {
            "description": "Consolidated schema information for the flow.\nContains input/output schemas, step output schemas, and shared `$defs`.",
            "allOf": [
              {
                "$ref": "#/components/schemas/FlowSchema"
              }
            ]
          },
          "steps": {
            "description": "The steps to execute for the flow.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Step"
            }
          },
          "output": {
            "description": "The outputs of the flow, mapping output names to their values.",
            "allOf": [
              {
                "$ref": "#/components/schemas/ValueExpr"
              }
            ]
          },
          "test": {
            "description": "Test configuration for the flow.",
            "oneOf": [
              {
                "$ref": "#/components/schemas/TestConfig"
              },
              {
                "type": "null"
              }
            ]
          },
          "examples": {
            "description": "Example inputs for the workflow that can be used for testing and UI dropdowns.",
            "type": [
              "array",
              "null"
            ],
            "items": {
              "$ref": "#/components/schemas/ExampleInput"
            }
          },
          "metadata": {
            "description": "Extensible metadata for the flow that can be used by tools and frameworks.",
            "type": "object",
            "additionalProperties": true
          }
        }
      },
      "FlowSchema": {
        "description": "Consolidated schema information for a flow.\n\nThis struct contains all schema/type information for the flow in a single location,\nallowing shared `$defs` across all schemas and avoiding duplication.\n\nSerializes as a valid JSON Schema with `type: \"object\"` and flow-specific\nproperties (`input`, `output`, `variables`, `steps`) under the `properties` key.",
        "type": "object",
        "properties": {
          "defs": {
            "description": "Shared type definitions that can be referenced by other schemas.\nReferences use the format `#/schemas/$defs/TypeName`.",
            "type": "object",
            "additionalProperties": {
              "$ref": "#/components/schemas/Schema"
            }
          },
          "input": {
            "description": "The input schema for the flow.",
            "oneOf": [
              {
                "$ref": "#/components/schemas/Schema"
              },
              {
                "type": "null"
              }
            ]
          },
          "output": {
            "description": "The output schema for the flow.",
            "oneOf": [
              {
                "$ref": "#/components/schemas/Schema"
              },
              {
                "type": "null"
              }
            ]
          },
          "variables": {
            "description": "Schema for workflow variables. This is a JSON Schema object where\nproperties define the available variables and their types.",
            "oneOf": [
              {
                "$ref": "#/components/schemas/Schema"
              },
              {
                "type": "null"
              }
            ]
          },
          "steps": {
            "description": "Output schemas for each step, keyed by step ID.\nNote: Step input schemas are not included here as they are\ncomponent metadata, not flow-specific schemas.\nUses IndexMap to preserve insertion order for deterministic serialization.",
            "type": "object",
            "additionalProperties": {
              "$ref": "#/components/schemas/Schema"
            }
          }
        },
        "required": [
          "defs",
          "steps"
        ]
      },
      "Step": {
        "description": "A step in a workflow that executes a component with specific arguments.\n\nNote: Step output schemas are stored in the flow's `types.steps` field,\nnot on individual steps. This allows for shared `$defs` and avoids duplication.",
        "type": "object",
        "properties": {
          "id": {
            "description": "Identifier for the step",
            "type": "string"
          },
          "component": {
            "description": "The component to execute in this step",
            "allOf": [
              {
                "$ref": "#/components/schemas/Component"
              }
            ]
          },
          "onError": {
            "oneOf": [
              {
                "$ref": "#/components/schemas/ErrorAction"
              },
              {
                "type": "null"
              }
            ]
          },
          "input": {
            "description": "Arguments to pass to the component for this step",
            "allOf": [
              {
                "$ref": "#/components/schemas/ValueExpr"
              }
            ]
          },
          "mustExecute": {
            "description": "If true, this step must execute even if its output is not used by the workflow output.\nUseful for steps with side effects (e.g., writing to databases, sending notifications).",
            "type": [
              "boolean",
              "null"
            ]
          },
          "metadata": {
            "description": "Extensible metadata for the step that can be used by tools and frameworks.",
            "type": "object",
            "additionalProperties": true
          }
        },
        "required": [
          "id",
          "component"
        ]
      },
      "ErrorAction": {
        "description": "Error action determines what happens when a step fails.",
        "oneOf": [
          {
            "$ref": "#/components/schemas/OnErrorFail"
          },
          {
            "$ref": "#/components/schemas/OnErrorDefault"
          },
          {
            "$ref": "#/components/schemas/OnErrorRetry"
          }
        ],
        "discriminator": {
          "propertyName": "action",
          "mapping": {
            "fail": "#/components/schemas/OnErrorFail",
            "useDefault": "#/components/schemas/OnErrorDefault",
            "retry": "#/components/schemas/OnErrorRetry"
          }
        }
      },
      "ValueExpr": {
        "oneOf": [
          {
            "title": "StepRef",
            "description": "Step reference: { $step: \"step_id\", path?: \"...\" }",
            "type": "object",
            "properties": {
              "$step": {
                "type": "string"
              },
              "path": {
                "description": "JSONPath expression",
                "type": "string"
              }
            },
            "required": [
              "$step"
            ],
            "additionalProperties": false
          },
          {
            "title": "InputRef",
            "description": "Workflow input reference: { $input: \"path\" }",
            "type": "object",
            "properties": {
              "$input": {
                "description": "JSONPath expression",
                "type": "string"
              }
            },
            "required": [
              "$input"
            ],
            "additionalProperties": false
          },
          {
            "title": "VariableRef",
            "description": "Variable reference: { $variable: \"path\", default?: ValueExpr }",
            "type": "object",
            "properties": {
              "$variable": {
                "description": "JSONPath expression including variable name",
                "type": "string"
              },
              "default": {
                "$ref": "#/components/schemas/ValueExpr"
              }
            },
            "required": [
              "$variable"
            ],
            "additionalProperties": false
          },
          {
            "title": "LiteralExpr",
            "description": "Escaped literal: { $literal: any }",
            "type": "object",
            "properties": {
              "$literal": {}
            },
            "required": [
              "$literal"
            ],
            "additionalProperties": false
          },
          {
            "title": "If",
            "description": "Conditional: { $if: condition, then: expr, else?: expr }",
            "type": "object",
            "properties": {
              "$if": {
                "$ref": "#/components/schemas/ValueExpr"
              },
              "then": {
                "$ref": "#/components/schemas/ValueExpr"
              },
              "else": {
                "$ref": "#/components/schemas/ValueExpr"
              }
            },
            "required": [
              "$if",
              "then"
            ],
            "additionalProperties": false
          },
          {
            "title": "Coalesce",
            "description": "Coalesce: { $coalesce: [expr, ...] }",
            "type": "object",
            "properties": {
              "$coalesce": {
                "type": "array",
                "items": {
                  "$ref": "#/components/schemas/ValueExpr"
                }
              }
            },
            "required": [
              "$coalesce"
            ],
            "additionalProperties": false
          },
          {
            "title": "ArrayExpr",
            "description": "Array with expressions as elements",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ValueExpr"
            }
          },
          {
            "title": "ObjectExpr",
            "description": "Object with expressions as values",
            "type": "object",
            "additionalProperties": {
              "$ref": "#/components/schemas/ValueExpr"
            }
          },
          {
            "title": "Literal",
            "description": "A literal JSON value (string, number, boolean, or null)",
            "oneOf": [
              {
                "type": "null"
              },
              {
                "type": "boolean"
              },
              {
                "type": "number"
              },
              {
                "type": "string"
              }
            ]
          }
        ]
      },
      "TestConfig": {
        "description": "Configuration for testing a workflow.",
        "type": "object",
        "properties": {
          "configFile": {
            "description": "Path to an external stepflow config file for tests.\nRelative paths are resolved from the workflow file's directory.\nMutually exclusive with `config` - validated at runtime.",
            "type": [
              "string",
              "null"
            ]
          },
          "config": {
            "description": "Inline stepflow configuration for tests.\nMutually exclusive with `config_file` - validated at runtime."
          },
          "cases": {
            "description": "Test cases for the workflow.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/TestCase"
            }
          }
        }
      },
      "TestCase": {
        "description": "A single test case for a workflow.",
        "type": "object",
        "properties": {
          "name": {
            "description": "Unique identifier for the test case.",
            "type": "string"
          },
          "description": {
            "description": "Optional description of what this test case verifies.",
            "type": [
              "string",
              "null"
            ]
          },
          "input": {
            "description": "Input data for the workflow in this test case.",
            "allOf": [
              {
                "$ref": "#/components/schemas/Value"
              }
            ]
          },
          "output": {
            "description": "Expected output from the workflow for this test case.",
            "oneOf": [
              {
                "$ref": "#/components/schemas/FlowResult"
              },
              {
                "type": "null"
              }
            ]
          }
        },
        "required": [
          "name",
          "input"
        ]
      },
      "ExampleInput": {
        "description": "An example input for a workflow that can be used in UI dropdowns.",
        "type": "object",
        "properties": {
          "name": {
            "description": "Name of the example input for display purposes.",
            "type": "string"
          },
          "description": {
            "description": "Optional description of what this example demonstrates.",
            "type": [
              "string",
              "null"
            ]
          },
          "input": {
            "description": "The input data for this example.",
            "allOf": [
              {
                "$ref": "#/components/schemas/Value"
              }
            ]
          }
        },
        "required": [
          "name",
          "input"
        ]
      },
      "DiagnosticLevel": {
        "description": "Diagnostic level indicating severity and impact\n\nCases:\n* `fatal`: Fatal: Prevents analysis from proceeding\n* `error`: Error: Will definitely fail during execution\n* `warning`: Warning: Likely to cause problems during execution",
        "type": "string",
        "enum": [
          "fatal",
          "error",
          "warning"
        ]
      },
      "ListStepRunsResponse": {
        "description": "Response for listing step runs",
        "type": "object",
        "properties": {
          "steps": {
            "description": "Dictionary of step run results keyed by step ID",
            "type": "object",
            "additionalProperties": {
              "$ref": "#/components/schemas/StepRunResponse"
            }
          }
        },
        "required": [
          "steps"
        ]
      },
      "StepRunResponse": {
        "description": "Response for step run details",
        "type": "object",
        "properties": {
          "stepId": {
            "description": "Step ID (the stable identifier for this step in the workflow)",
            "type": "string"
          },
          "component": {
            "description": "Component name/URL that this step executes",
            "type": [
              "string",
              "null"
            ]
          },
          "status": {
            "description": "Current status of the step",
            "allOf": [
              {
                "$ref": "#/components/schemas/StepStatus"
              }
            ]
          },
          "result": {
            "description": "The result of the step execution (if completed)",
            "oneOf": [
              {
                "$ref": "#/components/schemas/FlowResult"
              },
              {
                "type": "null"
              }
            ]
          }
        },
        "required": [
          "stepId",
          "status"
        ]
      },
      "StoreFlowRequest": {
        "description": "Request to store a flow",
        "type": "object",
        "properties": {
          "flow": {
            "description": "The flow to store",
            "allOf": [
              {
                "$ref": "#/components/schemas/Flow"
              }
            ]
          },
          "dryRun": {
            "description": "If true, only validate the flow without storing it",
            "type": "boolean",
            "default": false
          }
        },
        "required": [
          "flow"
        ]
      },
      "StoreFlowResponse": {
        "description": "Response when a flow is stored",
        "type": "object",
        "properties": {
          "flowId": {
            "description": "The ID of the flow (computed from content hash, always present)",
            "allOf": [
              {
                "$ref": "#/components/schemas/BlobId"
              }
            ]
          },
          "stored": {
            "description": "Whether the flow was actually stored (false for dry_run or if validation has fatal errors)",
            "type": "boolean"
          },
          "diagnostics": {
            "description": "Validation diagnostics",
            "allOf": [
              {
                "$ref": "#/components/schemas/Diagnostics"
              }
            ]
          }
        },
        "required": [
          "flowId",
          "stored",
          "diagnostics"
        ]
      },
      "Diagnostics": {
        "description": "Collection of diagnostics with utility methods",
        "type": "object",
        "properties": {
          "diagnostics": {
            "description": "All diagnostics found",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Diagnostic"
            }
          },
          "numFatal": {
            "type": "integer",
            "format": "uint32",
            "minimum": 0
          },
          "numError": {
            "type": "integer",
            "format": "uint32",
            "minimum": 0
          },
          "numWarning": {
            "type": "integer",
            "format": "uint32",
            "minimum": 0
          }
        },
        "required": [
          "diagnostics",
          "numFatal",
          "numError",
          "numWarning"
        ]
      }
    }
  },
  "tags": [
    {
      "name": "Blob",
      "description": "Blob API endpoints"
    },
    {
      "name": "Component",
      "description": "Component API endpoints"
    },
    {
      "name": "Flow",
      "description": "Flow API endpoints"
    },
    {
      "name": "Run",
      "description": "Run API endpoints"
    }
  ]
}